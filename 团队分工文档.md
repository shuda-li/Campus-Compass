# Campus Compass — MVP 实操分工计划

> 版本：v2.0（实操版）  
> 日期：2026-04-27  
> 核心理念：**每个任务都告诉你打开哪个文件、改哪一行、改什么**

---

## 🌍 全员 30 秒快速启动

```bash
# 1. 克隆仓库
git clone https://github.com/shuda-li/Campus-Compass.git
cd Campus-Compass

# 2. 创建虚拟环境并安装依赖
python -m venv venv
venv\Scripts\activate        # Windows
pip install -r requirements.txt

# 3. 初始化数据库
python data/init_db.py

# 4. 启动服务
python web/app.py
# → 浏览器打开 http://localhost:5000
```

---

## 📁 项目当前状态速查

```
campus-compass/                 # 22 个文件，全部已创建 + 可运行
├── .gitignore                  # Git 忽略规则（已配好）
├── requirements.txt            # Flask, requests, python-dotenv
├── config.py                   # API Key / 路径配置（从 .env 读取）
├── llm_client.py               # OpenAI API 封装（含兜底，76 行）
├── main.py                     # 命令行模式入口
├── formatter.py                # HTML + Markdown 双格式输出
│
├── data/
│   ├── init_db.py              # SQLite 建表 + 7 间示例教室
│   └── templates.json          # 4 套活动模板 JSON
│
├── engine/                     # Agent 核心大脑
│   ├── __init__.py             # 空包文件
│   ├── workflow.py             # 8 步主工作流（60 行）
│   ├── intent_parser.py        # 规则引擎：关键词 + 正则提取
│   ├── template_matcher.py     # 活动模板加载
│   ├── room_scorer.py          # 教室多维评分排序（v2：比例分档）
│   └── completeness_checker.py # 信息完整性评估（备用，当前未启用）
│
├── tools/                      # 纯函数工具层
│   ├── __init__.py             # 空包文件
│   ├── db_service.py           # SQLite 查询服务
│   ├── navigation.py           # 导航文本生成（17 行）
│   └── budget_calc.py          # 预算计算器（22 行）
│
└── web/
    ├── app.py                  # Flask 主程序（人数预问机制已启用）
    └── templates/
        └── index.html          # TailwindCSS 深色风 UI（228 行）
```

---

## 👥 5 人分工 — 实操版

### 成员 1：Web 前端 + 展示层

**负责文件（3 个）**：

| 文件 | 作用 | 当前状态 |
|------|------|:--:|
| `web/templates/index.html` | 聊天界面（TailwindCSS 深色风） | ✅ 已可运行 |
| `web/app.py` | Flask 路由（含人数预问） | ✅ 已可运行 |
| `formatter.py` | HTML 策划书格式化 | ✅ 与前端 CSS 对齐 |

---

#### 🔧 第 1 周任务：理解 + 微调

**任务 1.1：跑通项目，熟悉每条路由**

```bash
python web/app.py
# 浏览器打开 http://localhost:5000
# 输入 "我想办一个50人的技术讲座" → 看输出
```

**任务 1.2：理解 `formatter.py` 与 `index.html` 的 CSS 对应关系**

| formatter.py 生成的 CSS class | index.html 中的样式定义位置 | 作用 |
|---|---|---|
| `bg-darkCard` | Tailwind 配置的 `darkCard: '#1E1E1E'` | 策划书卡片背景 |
| `bg-pinkMuted` | `pinkMuted: 'rgba(242,160,181,0.15)'` | 浅粉背景区 |
| `text-pink` | `pink: '#F2A0B5'` | 粉色文字高亮 |
| `border-pink/30` | Tailwind 透明度工具类 | 粉色半透明边框 |

**任务 1.3：把欢迎页的 4 个快捷按钮替换为你们学校的真实场景**

打开 `web/templates/index.html` 第 80-104 行，修改 4 个 `<button onclick="...">` 的文本和内容。

---

#### 🔧 第 2 周任务：交互优化

**任务 2.1：测试人数预问机制**

```
场景 A：输入 "办个技术讲座" → 应该弹出"预计多少人参加？"
场景 B：回复 "80" → 应该直接生成策划书
场景 C：回复 "跳过" → 应该用默认 50 人直接生成
```

**任务 2.2：优化移动端显示**

打开 `index.html` 搜索 `@media`，有两个断点：768px（平板）和 480px（手机）。
在手机浏览器上打开 `http://你的电脑IP:5000` 测试。

**任务 2.3：美化策划书内的时间线样式**

策划书中的时间线在 `formatter.py` 第 66-73 行生成，圆点和竖线用 CSS `border-l-2` 实现。
如果觉得样式不够好，调整第 66 行的 CSS class。

---

#### 🔧 第 3-4 周任务：联调 + 打磨

- 与成员 2 联调：确保 `web/app.py` → `run_workflow()` 调用正常
- 成员 4 录入真实教室数据后，验证策划书中的教室卡片渲染
- 成员 5 配置 LLM 后，验证 AI 生成的标题和时间线在页面中的展示

**交付物**：
- [ ] 可在浏览器正常使用的聊天界面
- [ ] 手机端布局不崩
- [ ] 策划书全部内容（时间线/预算/教室/导航）正确渲染

---

### 成员 2：引擎核心 + 工作流

**负责文件（3 个）**：

| 文件 | 作用 | 当前状态 |
|------|------|:--:|
| `engine/workflow.py` | 8 步主工作流（串联协调） | ✅ |
| `engine/intent_parser.py` | 意图解析器（规则引擎） | ✅ |
| `engine/__init__.py` | 空包文件 | ✅ |

---

#### 🔧 第 1 周任务：画出数据流图

阅读 `engine/workflow.py` 第 11-52 行，画出 A4 纸大小的数据流图：

```
用户输入 → parse_intent() → intent → match_template() → template
    → call_llm() → ideas → _build_plan() → plan
    → query_rooms() → rooms → rank_rooms() → sorted
    → generate_navigation() → budget → build_html() → HTML
```

每个箭头标注数据类型（dict / list / str）。

---

#### 🔧 第 2 周任务：扩充关键词词典

打开 `engine/intent_parser.py` 第 3-8 行。

当前只有 4 种类型、27 个中文关键词。扩充到至少 40 个关键词，覆盖更多说法：

```python
# 当前代码（第 3-8 行）
ACTIVITY_KEYWORDS = {
    "讲座": ["讲座","演讲","分享","学术","报告","seminar","交流会","研讨会"],
    "晚会": ["晚会","演出","文艺","才艺","迎新","毕业","联欢","庆典","party"],
    "竞赛": ["竞赛","比赛","大赛","挑战赛","编程","辩论","答辩","contest"],
    "社团活动": ["社团","团建","聚会","沙龙","工作坊","workshop","见面会"],
}

# 👆 你需要在每个列表里加词，比如"论坛""峰会""路演""体育""志愿者"... 
```

第 45 行的设备关键词列表也检查一下，是否需要加你们学校常见的设备名。

---

#### 🔧 第 3 周任务：测试工作流鲁棒性

用以下输入在终端跑 `python main.py`，观察输出：

| 输入 | 预期结果 |
|------|---------|
| "讲座" | 识别为讲座，默认 50 人 |
| "帮我在D座找个教室办个晚会100人" | 晚会 / 100人 / D座 |
| ""（空输入） | 不崩溃 |
| "asdfghjkl12345" | 兜底为讲座 50 人 |

---

#### 🔧 第 4 周任务：全链路联调

确保你的工作流能依次调用成员 3/4/5 的模块不出错。

**交付物**：
- [ ] 数据流图（手绘或画图软件）
- [ ] 关键词词典扩充至 ≥ 40 词
- [ ] 5 种输入测试通过截图

---

### 成员 3：模板匹配 + 教室推荐算法

**负责文件（3 个）**：

| 文件 | 作用 | 当前状态 |
|------|------|:--:|
| `engine/template_matcher.py` | 从 JSON 加载活动模板 | ✅ |
| `engine/room_scorer.py` | 教室多维评分排序（v2） | ✅ 比例分档版 |
| `data/templates.json` | 4 套活动模板数据 | ✅ |

---

#### 🔧 第 1 周任务：理解评分算法

打开 `engine/room_scorer.py` 阅读全文（76 行）。

当前算法 4 个维度：

```
容量适配度（0~15）：按余量比例分档——刚好 8 / 最佳 15 / 偏大 12 / 过大 5
楼层便利性（0~5）：  1-2 楼 +5，3-4 楼 +2
设备匹配度（0~15）：每匹配一项 +3
建筑偏好  （0~10）：匹配用户指定的建筑 +10
```

**关键理解**：每个维度的分值和权重是否合理？写出你的分析（1 页）。

---

#### 🔧 第 2 周任务：丰富模板内容

打开 `data/templates.json`（150 行），当前有讲座/晚会/竞赛/社团活动 4 套模板。

**你需要做的**：

1. 检查每个模板的时间线是否合理（比如讲座真的有"茶歇饮料"预算吗？）
2. 如果有的话，添加第 5 种活动类型（如"体育活动""志愿者活动"）
3. 调整各模板的默认预算，使数字更贴近你们学校的实际情况

---

#### 🔧 第 3-4 周任务：验证 + 与成员 4 联调

1. 等待成员 4 完成真实教室数据录入
2. 手动测试：假如 50 人需要投影 + 偏好 D 座，哪个教室排第一？合理吗？
3. 如果排名不符合直觉，调整 `room_scorer.py` 中的分值参数

**交付物**：
- [ ] 评分算法分析（1 页）
- [ ] 模板时间线 / 预算经过审核
- [ ] 教室排序结果验证截图

---

### 成员 4：工具层 + 数据层

**负责文件（5 个）**：

| 文件 | 作用 | 当前状态 |
|------|------|:--:|
| `tools/db_service.py` | SQLite 查询（47 行） | ✅ |
| `tools/navigation.py` | 导航文本生成（29 行） | ✅ |
| `tools/budget_calc.py` | 预算计算器（22 行） | ✅ |
| `data/init_db.py` | 建表 + 示例数据（81 行） | ⚠️ 7 间示例教室 |
| `tools/__init__.py` | 空包文件 | ✅ |

---

#### 🔧 第 1-2 周核心任务：录入真实教室数据 🔥

这是整个项目**最关键的实操任务**。打开 `data/init_db.py` 第 29-65 行，当前有 7 间示例教室。

**你需要做的**：清空 7 间示例数据，换成你们学校的真实教室（≥ 10 间）。

**每条数据需要填 10 个字段**：

```python
("教室编号",       # 如 "A201"（真实编号）
 "建筑名+教学楼",   # 如 "思源书院D座"
 楼层数字,         # 如 2
 容纳人数,         # 如 80
 面积m²,           # 如 100.0
 json.dumps(["设备1","设备2"], ensure_ascii=False),  # 真实设备
 "从XX进入，...",   # 入口指引（具体文字）
 json.dumps(["参照物1","参照物2"], ensure_ascii=False),  # 导航参照物
 X坐标,            # 从中心点起算（整数即可）
 Y坐标             # 从中心点起算（整数即可）
),
```

**坐标怎么定义？**

在你校园地图上画一个坐标系，以图书馆 / 主教学楼/广场为原点 (0,0)，其他建筑根据相对位置估算坐标。精度不重要，相对关系重要。

**示例**（对照格式）：

```python
("B301", "博学楼B座", 3, 120, 150.0,
 json.dumps(["投影仪","音响","空调","白板"], ensure_ascii=False),
 "从博学楼大厅乘电梯到3楼，出电梯右转",
 json.dumps(["三楼电梯口","右转第一间"], ensure_ascii=False),
 300, 250),
```

---

#### 🔧 第 2-3 周任务：优化导航文本

打开 `tools/navigation.py`（29 行）。

当前逻辑是从数据库读取 `entrance_note` 和 `nav_landmarks` 拼接。如果你的导航文本够详细了，可以不改代码。如果觉得输出太简单，在 17-29 行之间加更多拼接逻辑（比如增加"预计步行时间"）。

---

#### 🔧 第 3 周任务：优化预算标准

打开 `tools/budget_calc.py`（22 行）。

当前逻辑：
- ≤50 人 → 费用 × 1.0
- 51-100 人 → 费用 × 1.2
- >100 人 → 费用 × 1.5

如果你们学校有具体的场地租赁费 / 设备使用费标准，调整第 12-14 行的 `multiplier` 值。

---

#### 🔧 第 4 周任务：数据库上线 + 联调

1. 运行 `python data/init_db.py` 确认无报错
2. 启动 Flask，输入活动描述，验证你的教室能出现在推荐列表里
3. 与成员 3 联调：教室排序结果是否符合预期

**交付物**：
- [ ] `data/init_db.py` 中 ≥ 10 间真实校园教室数据
- [ ] 每间教室的导航文本可读
- [ ] 预算费率经过调整

---

### 成员 5：LLM 集成 + 全栈串联 + 文档

**负责文件（6 个）**：

| 文件 | 作用 | 当前状态 |
|------|------|:--:|
| `llm_client.py` | OpenAI API 封装 | ✅ 已写好兜底 |
| `config.py` | 配置管理 | ✅ |
| `main.py` | 命令行入口 | ✅ |
| `requirements.txt` | 依赖清单 | ✅ |
| `.gitignore` | Git 忽略规则 | ✅ |
| `.env` | API Key（不提交 Git） | ⚠️ 需要创建 |

---

#### 🔧 第 1 周核心任务：配通 LLM 🔥

**Step 1**：在项目根目录创建 `.env` 文件：

```
LLM_API_KEY=sk-你的OpenAI密钥
LLM_API_URL=https://api.openai.com/v1/chat/completions
LLM_MODEL=gpt-3.5-turbo
```

**Step 2**：测试 LLM 调用：

```bash
python main.py
> 我想办一个50人的技术讲座
```

观察终端日志 `[Step 2]` 是否输出了 LLM 生成的活动标题。如果它显示的是"讲座策划方案"（兜底文本），说明 LLM 没通。

**Step 3**：如果不用 OpenAI，用其他兼容 API（如 DeepSeek）：

打开 `config.py` 第 7-8 行，修改：

```python
LLM_API_URL = "https://api.deepseek.com/v1/chat/completions"
LLM_MODEL = "deepseek-chat"
```

打开 `llm_client.py` 第 20 行，检查请求格式是否需要适配。

---

#### 🔧 第 2 周任务：优化提示词 + 兜底逻辑

打开 `llm_client.py` 第 38-59 行（提示词构建）。

**当前提示词**：

```python
请根据以下信息生成校园活动方案:
活动类型: {activity_type}
预计人数: {participants}人
用户想法: {theme}
```

**你需要测试**：用 `python main.py` 输入不同的活动类型，观察 LLM 返回的时间线、标题是否合理。如果不合理，调整提示词的措辞。

打开 `llm_client.py` 第 71-80 行（兜底逻辑）：

如果 LLM 挂了，当前兜底返回的是固定 3 段时间线。考虑是否要根据活动类型返回不同的兜底内容？

---

#### 🔧 第 3-4 周任务：全栈串联 + 演示准备

**任务 3.1：端到端测试**

用 `python main.py` 和浏览器各跑一遍完整流程，记录 Bug。

**任务 3.2：准备演示脚本**

写一个 3 分钟的演示流程：

```
1. 打开 http://localhost:5000
2. 输入 "我想办一个 80 人的晚会，需要舞台灯光" → 展示策划书
3. 输入 "在 D 座办个技术沙龙" → 展示人数询问 → 回复 "60" → 展示结果
4. 重点讲解教室评分排名的算法逻辑（为什么推荐 D206 而不是 D101）
```

**任务 3.3：制作演示 PPT**（5-6 页）

```
第 1 页：项目介绍 — Campus Compass 是什么
第 2 页：技术架构 — 4 层架构图 + 8 步工作流
第 3 页：核心亮点 — 确定性引擎 × LLM 辅助 vs 纯 LLM Agent
第 4 页：教室评分算法 — 4 维度 × 比例分档
第 5 页：演示截图 — 输入 → 输出效果
第 6 页：分工与鸣谢
```

**交付物**：
- [ ] `.env` 文件已创建，LLM 调用成功
- [ ] 提示词优化后返回质量满意
- [ ] 3 分钟演示脚本
- [ ] 5-6 页演示 PPT

---

## 📋 开发规范（全员遵守）

### Git 分支

```bash
# 每人从 main 创建自己的分支
git checkout -b dev-member1   # 成员 1
git checkout -b dev-member2   # 成员 2
git checkout -b dev-member3   # 成员 3
git checkout -b dev-member4   # 成员 4
git checkout -b dev-member5   # 成员 5

# 完成后推送到 GitHub
git add -A
git commit -m "[模块] 简短描述"
git push origin dev-memberX
```

### 提交信息格式

```
[web]  修复手机端消息气泡溢出
[engine] 扩充 intent_parser 关键词词典至 42 词
[data]  录入 12 间真实教室数据
[tools] 调整预算费率系数
```

### 测试规范

```bash
# 每次改完代码后必须运行
python main.py           # 命令行模式测试
python web/app.py        # Web 模式测试
```

---

## ⚠️ 常见踩坑

| 症状 | 原因 | 解决 |
|------|------|------|
| `ModuleNotFoundError: No module named 'flask'` | 未激活虚拟环境 | `venv\Scripts\activate` |
| 网页打开白屏 | Flask 没启动或端口冲突 | 检查终端是否有 `Running on http://127.0.0.1:5000` |
| LLM 返回兜底文本 | API Key 未配置 | 创建 `.env` 文件填入 `LLM_API_KEY` |
| `sqlite3.OperationalError: no such table: rooms` | 数据库未初始化 | `python data/init_db.py` |
| `json.decoder.JSONDecodeError` | `templates.json` 格式错误 | 检查最后有没有多余逗号 |

---

## ✅ 签字确认

| 成员 | 姓名 | 负责模块 | 签字 | 日期 |
|------|------|---------|------|------|
| 成员 1 | | Web 前端 + 展示层 | | |
| 成员 2 | | 引擎核心 + 工作流 | | |
| 成员 3 | | 模板 + 教室评分 | | |
| 成员 4 | | 工具层 + 数据层 | | |
| 成员 5 | | LLM + 全栈 + 文档 | | |

---

> **这份文档的使用方式**：打印出来，每人拿自己那页，对着文件路径和行号直接开始干活。
