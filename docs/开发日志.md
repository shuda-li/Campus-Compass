# Campus Compass 开发日志

---

## 2026-05-07 — 项目质量评估与问题清单

> 评估人：全员  
> 评估依据：Agent 教学框架（六期）+ Agent / Skill / Harness 三层架构  
> 评估范围：全部 32 个源文件  
> 综合得分：**11 / 60**

---

### 一、架构定位问题

#### ❌ P0 — 系统不是 Agent，是流水线

`agent/workflow.py` 是一条硬编码的 9 步线性流水线。LLM 不驱动工具调用、不做决策、不管反馈。模型只是一个"文笔助手"，在 Step 2 被叫一次，输出一段文案，然后流程头也不回地走下去。

- 没有 Thought → Action → Observation → 再 Thought 的循环
- 如果教室查询返回 0 间，流程不回头、不让模型重新决策
- 本质是"带 LLM 文案的规则引擎 + 数据处理流水线"，不是 Agent

#### ❌ P0 — 两套方案生成路径字段不兼容

| | `agent/workflow.py`（命令行） | `web/app.py`（Web 端） |
|---|---|---|
| LLM 调用 | `llm_client.call_llm_for_ideation` | `plan_generator.call_llm_for_plan` |
| 方案字段 | `title` / `timeline` / `resources` / `promotion` | `activity_purpose` / `activity_content` / `activity_materials` |
| `agent/formatter.py` 期望 | ❌ 不匹配 → 活动目的/内容/物资永远为空 | ✅ 匹配 |

**影响**：命令行模式生成的 HTML 策划书缺少活动目的、活动内容和物资清单。

---

### 二、记忆系统问题

#### ❌ P0 — L3 长期记忆完全缺失

- 没有跨会话持久化用户偏好（常用建筑、惯用人数、历史选择）
- 用户每次打开网页，Agent 都不认识她

#### ❌ P0 — 没有 Token 预算管理

- 无 `trim_to_budget()` 函数
- 无任何上下文字数/轮数限制
- 无 L2 摘要压缩逻辑

#### ⚠️ P1 — L1 内存 Session 重启即丢

- `web/app.py` 的 `sessions = {}` 是普通 Python dict
- Flask 重启 → 所有用户对话状态归零

---

### 三、任务规划问题

#### ❌ P0 — 没有 TodoWrite 动态规划能力

- 9 步固定流水线 = 计划硬编码在 `agent/workflow.py` 里
- 模型无法自主拆解复杂任务
- 没有 `state.todos`、没有进度跟踪

#### ⚠️ P1 — `completeness_checker.py` 是死代码

- 199 行完整的渐进式追问模块，从未被任何文件 import 或使用

---

### 四、Harness 层问题

#### ❌ P0 — 可观测性几乎为零

- 日志：`print("[Step N] xxx")` — 松散的 stdout，无结构化 trace
- 失败处理：多处 `except Exception: pass` 直接吞异常
- 无评估打分、无回滚机制

#### ❌ P0 — 安全边界不存在

- 无工具白名单
- 无路径锁/沙箱
- 无并发控制 / rate limiting
- 仅 `.env` API Key 隔离合格

---

### 五、算法 & 功能问题

#### ⚠️ P1 — 教室评分存在硬跳变

- 容量评分采用分段阶梯式，边界 1% 差异可造成 7 分跳变
- 应采用连续评分函数（如高斯分布）

#### ⚠️ P1 — Sub-Agent / Agent Team 完全缺失

- 无上下文隔离、无子代理白名单、无 JSONL inbox、无花名册

---

### 六、代码质量问题

#### ⚠️ P1 — LLM 调用散落 4 个文件

- `agent/llm_client.py` / `engine/plan_generator.py` / `engine/topic_analyzer.py` 各自 import `requests`
- 超时设置不统一（30s / 25s / 15s）
- 无统一重试逻辑

#### ⚠️ P2 — data/init_db.py 每次运行都尝试 INSERT

- 靠 `IntegrityError` 被忽略兜底
- 应改为 `INSERT OR IGNORE`

#### ⚠️ P2 — 欢迎页按钮文案错误

- 第 5 个按钮文字"社团沙龙"，实际发送"校园摄影展"

#### ⚠️ P2 — 占位符硬编码

- `activity_time` / `organizer` / `host` = "XXX"

---

### 问题汇总

| 编号 | 严重度 | 模块 | 问题 |
|------|:--:|------|------|
| P0-01 | 🔴 | agent/workflow | 不是 Agent，是线性流水线 |
| P0-02 | 🔴 | workflow / web | 两套方案生成字段不兼容 |
| P0-03 | 🔴 | memory | L3 长期记忆缺失 |
| P0-04 | 🔴 | agent/workflow | 无 Token 预算管理 |
| P0-05 | 🔴 | agent/workflow | 无 TodoWrite 动态规划 |
| P0-06 | 🔴 | 全局 | 可观测性几乎为零 |
| P0-07 | 🔴 | 全局 | Harness 安全边界不存在 |
| P1-01 | 🟡 | web/app | Session 重启即丢 |
| P1-02 | 🟡 | engine/ | completeness_checker 死代码 |
| P1-03 | 🟡 | engine/room_scorer | 评分硬跳变 |
| P1-04 | 🟡 | 全局 | Sub-Agent / Team 缺失 |
| P1-05 | 🟡 | 全局 | LLM 调用散落 4 文件 |
| P2-01 | 🟢 | data/init_db | INSERT 应改 INSERT OR IGNORE |
| P2-02 | 🟢 | web/index.html | 按钮文案错误 |
| P2-03 | 🟢 | engine/plan_generator | "XXX"占位符 |

---

### 下一步建议

1. **先修 P0-01 + P0-05**：重构 workflow 为真正的 Agent Loop，引入 TodoWrite
2. **再修 P0-02**：统一两条方案生成路径
3. **然后修 P0-03 + P0-04**：补齐记忆系统和 Token 管理
4. **最后修 P0-06 + P0-07**：加固 Harness 层

---

## 2026-05-07 — P0-01 + P0-02 修复：重构为真正的 Agent Loop

### 修复内容

#### P0-01：系统不是 Agent，是流水线 → ✅ 已修复

**问题**：`agent/workflow.py` 是硬编码 9 步流水线，LLM 只做文案不参与决策。

**方案**：新建 `agent/agent_loop.py`，实现两套运行模式：

| 模式 | 触发条件 | 行为 |
|------|---------|------|
| **Agent 模式** | 有 LLM API Key | LLM 作为决策引擎，通过 OpenAI Function Calling 自主选择工具调用顺序，每轮 Thought → Action → Observation → 再 Thought 闭环 |
| **降级模式** | 无 LLM API Key | 确定性流水线（仍用 `plan_generator` 生成完整方案） |

**架构变化**：
- 新增 `agent/agent_loop.py`（~500 行）：Agent Loop + 8 个工具定义 + AgentState + 降级流水线
- 更新 `main.py`：入口改为 `from agent.agent_loop import run_agent`
- 保留 `agent/workflow.py`：后续可删除（已无引用）

**8 个工具**：
1. `parse_user_input` — 意图解析
2. `analyze_and_expand_topic` — 主题分析扩展
3. `generate_activity_plan` — 方案生成
4. `find_classrooms` — 教室查询
5. `score_classrooms` — 教室评分排序
6. `get_navigation` — 导航指引
7. `calculate_budget` — 预算计算
8. `finalize` — 输出最终 HTML

**关键设计决策**：
- `AgentState` 独立于 messages，工具读写 state，LLM 只看到工具返回的摘要
- 每个工具返回精简 JSON 摘要，避免上下文膨胀
- 如果 LLM 循环结束但未调 finalize，自动用已有数据生成 HTML

#### P0-02：两套方案生成字段不兼容 → ✅ 已修复

**问题**：命令行用 `llm_client.call_llm_for_ideation`（字段：title/timeline/resources），Web 用 `plan_generator.generate_plan`（字段：activity_purpose/content/materials）。

**方案**：降级流水线统一使用 `plan_generator.generate_plan`。现在命令行和 Web 走同一套方案生成逻辑，输出包含完整的活动目的、内容环节、物资清单。

### 测试结果

- ✅ 降级模式：`"50人的技术讲座"` → 输出 5 环节 + 6 物资 + 活动目的（200-300字）
- ⏳ Agent 模式：待配置 API Key 后验证

### 文件变更

| 文件 | 操作 |
|------|------|
| `agent/agent_loop.py` | 新建 |
| `main.py` | 修改入口 |
| `agent/workflow.py` | 保留（后续可删） |

---

## 2026-06-02 — P0+P1：切换 DeepSeek API + Agent Loop 缓存优先重构

> 参考：DeepSeek-Reasonix 的 "Cache-First Loop" 架构  
> 动机：DeepSeek 前缀缓存需要字节级前缀完全匹配，原有 nag 注入 + trim 裁剪会持续破坏缓存

---

### P0：LLM 后端切换至 DeepSeek API

**变更**：

| 文件 | 操作 | 说明 |
|------|------|------|
| `.env` | 新建 | DeepSeek API 配置（`deepseek-chat` 模型） |
| `config.py` | 修改 | 默认 `LLM_API_URL` 和 `LLM_MODEL` 改为 DeepSeek |
| `.env.example` | 修改 | DeepSeek 提升为推荐配置，OpenAI 降为备选 |

**配置**：
```
LLM_API_URL=https://api.deepseek.com/v1/chat/completions
LLM_MODEL=deepseek-chat
```

Campus Compass 使用原始 `requests.post()` + OpenAI 兼容格式，DeepSeek API 完全兼容，无需修改任何 LLM 调用代码。

---

### P1：Agent Loop 缓存优先（Cache-First）重构

参考 Reasonix 的四支柱架构，将 Agent Loop 改造为 **"不可变前缀 + 追加日志 + 易变暂存"** 三层模型：

**架构变化**：

```
不可变前缀（Immutable Prefix）     ← 永远不修改，前缀缓存 100% 命中
  ├─ System Prompt（含记忆块）
  └─ 第一条用户输入

追加日志（Append-Only Log）        ← 只追加不删除，保护后续缓存
  ├─ LLM 回复 + 工具调用
  └─ 工具返回结果

易变暂存（Volatile Scratch）       ← 系统干预统一收集，作为单条消息追加
  └─ Todo Nag / 预算警告
```

**具体修改**：

#### 1. `agent/memory/trim.py` — 全面重构

| 函数 | 变更 | 说明 |
|------|------|------|
| `trim_to_budget()` | **改为 no-op** | 旧版从头部删除消息会破坏全部前缀缓存，现在不做任何裁剪 |
| `soft_trim_check()` | **新增** | 分层检查：软预算线（8000 tokens）仅警告不删除；硬预算线（64000 tokens）触发一次性压缩 |
| `hard_trim_to_budget()` | **新增** | 紧急压缩：保留 system 消息 + 最近 ~4000 tokens，中间部分用摘要替代。仅在接近模型上限时触发（正常 15 轮 Agent 循环不会触及 DeepSeek 128K 上限） |
| `DEFAULT_TOKEN_BUDGET` | 保留 | 8000 tokens（软预算线，触发效率提醒） |
| `HARD_TOKEN_BUDGET` | **新增** | 64000 tokens（硬预算线，触发一次性压缩） |

**为什么 8000 → 64000 有这么大差距**：DeepSeek V4 上下文窗口为 128K tokens。15 轮 Agent 循环（每轮：user msg ~50 + assistant ~200 + tool result ~200）≈ 6750 tokens，远低于 64K 硬预算。正常情况完全不需要裁剪，前缀缓存持续命中。

#### 2. `agent/agent_loop.py` — Scratch 机制重构

**问题 → 修复**：

| 问题 | 旧行为 | 新行为 |
|------|--------|--------|
| Todo Nag 注入 | `messages.append(nag)` 在循环中间，位置随条件漂移 | `_collect_scratch_notes()` 收集所有 nag，`_flush_scratch()` 统一追加到末尾 |
| Token 预算裁剪 | 每轮 `trim_to_budget()` 从头部删消息 | 仅在 `should_hard_trim=True` 时做一次性压缩（正常不触发） |
| 无工具调用 Nag | 立即 `messages.append(nag)` | 仍然立即追加（需要 LLM 纠正），但追加位置固定（末尾） |
| 多 nag 导致碎片化 | 每个 nag 单独一条 user 消息 | 所有 scratch 合并为一条 user 消息追加 |

**新增函数**：
- `_collect_scratch_notes(state, messages)` → `list[tuple]`：收集本轮所有系统干预
- `_flush_scratch(messages, scratch_notes, trace)` → `list`：合并 scratch 为单条消息追加

**核心原则**：
1. ✅ 永远不删除消息（保护前缀缓存）
2. ✅ 系统干预统一收集到 scratch，作为单条消息追加
3. ✅ 仅在线性预算临界时做一次性压缩（而非每轮裁剪）
4. ✅ 工具定义作为 API 参数传递，不在消息体内（DeepSeek 按完整 payload 做前缀匹配）

---

### 缓存命中率预期

| 场景 | 旧架构（trim + nag 注入） | 新架构（Cache-First） |
|------|--------------------------|----------------------|
| System Prompt | 100%（除非 trim 触发） | **100%**（不可变前缀） |
| 前 3 轮对话 | <20%（trim 频繁破坏前缀） | **~95%**（追加不删） |
| 正常 15 轮 | ~0%（每轮 trim 重置前缀） | **~85-90%**（仅 nag 处断点） |
| 极端长对话 | — | 一次性断裂（hard_trim），约 0.1% 场景触发 |

---

### 文件变更汇总

| 文件 | 操作 | 说明 |
|------|------|------|
| `.env` | **新建** | DeepSeek API 配置 |
| `config.py` | 修改 | 默认 URL/Model 改为 DeepSeek |
| `.env.example` | 修改 | DeepSeek 提升为首选 |
| `agent/memory/trim.py` | **重构** | 新增 `soft_trim_check`、`hard_trim_to_budget`；`trim_to_budget` 改为 no-op |
| `agent/agent_loop.py` | **重构** | 新增 scratch 收集/刷新机制；替换 trim 调用为分层检查 |

---

### 待办

- [x] 填入有效的 DeepSeek API Key 后验证 Agent 模式（2026-06-02 已配置 `sk-c1ef...`，连通性测试通过 ✅）
- [ ] 验证缓存命中率（需在 DeepSeek API Console 查看 `prompt_cache_hit_tokens`）
- [ ] 长期监控 `hard_trim_to_budget` 触发频率（目标：正常使用中 0 次触发）

---

## 2026-06-02（续）— 代理默认关闭 + 空白地址 修复

### 变更原因

- 原有代理默认 `http://127.0.0.1:7897` 为个人开发环境地址，对其他用户不可达
- 代理默认开启会导致无代理环境的 API 调用全部失败（非必要障碍）
- 参考 Reasonix 的 "零配置启动" 理念，代理应该是 opt-in 而非 opt-out

### 修改文件

| 文件 | 位置 | 旧值 | 新值 |
|------|------|------|------|
| `agent/proxy.py:5` | `_default_proxy` | `'http://127.0.0.1:7897'` | `''`（空字符串） |
| `web/app.py:56` | session 初始状态 | `proxy_enabled: True` | `proxy_enabled: False` |
| `web/app.py:57` | session 初始状态 | `proxy_address: 'http://127.0.0.1:7897'` | `proxy_address: ''` |
| `web/app.py:64` | `_apply_proxy` 默认值 | `state.get("proxy_enabled", True)` | `state.get("proxy_enabled", False)` |
| `web/app.py:158-159` | GET `/api/proxy` 有 session | `True` / `'http://127.0.0.1:7897'` | `False` / `''` |
| `web/app.py:161-164` | GET `/api/proxy` 无 session | `True` / `'http://127.0.0.1:7897'` | `False` / `''` |
| `web/templates/index.html:308-309` | JS 变量初始值 | `proxyEnabled = true` / `proxyAddress = "http://..."` | `proxyEnabled = false` / `proxyAddress = ""` |
| `web/templates/index.html:317-318` | JS 读取 localStorage | `cfg.enabled !== false` / `cfg.address \|\| "http://..."` | `cfg.enabled !== undefined ? cfg.enabled : false` / `cfg.address \|\| ""` |

### 用户影响

- ✅ 全新安装不配置代理即可直接使用 DeepSeek API（无需在 UI 中手动关闭代理）
- ✅ 之前保存过代理设置的用户不受影响（localStorage 优先）
- ✅ 页面右上角代理开关状态指示灯：默认灰色（关闭）而非绿色（开启）

---

## 2026-05-07 — P0-03～P0-07 修复：Harness 层全面加固

### 修复内容总览

基于 What_is_agent.md 的 Harness 定义（Harness = `agent_loop` + `registry` + `trim` + `persistence` + `config` + `trace`），一次性补齐所有缺失的 Harness 模块。

#### P0-03：L3 长期记忆缺失 → ✅ 已修复

**新增** `agent/memory/persistence.py`：
- `.memory/index.json` 索引 + `.memory/{id}.json` 单条记忆
- `save_preference()` — 保存用户偏好（建筑/人数/活动类型）
- `load_memory_block()` — 启动时注入 system prompt
- `auto_remember()` — finalize 时自动记录上次使用的建筑、人数、类型
- 新增工具 `save_user_preference` — Agent 可主动保存用户偏好

#### P0-04：无 Token 预算管理 → ✅ 已修复

**新增** `agent/memory/trim.py`：
- `estimate_tokens(messages)` — 按 1 token ≈ 3 chars 近似估算
- `trim_to_budget(messages, budget=8000)` — 系统消息永久保留，旧轮次从头部裁
- 每轮 Agent 循环自动调用 `trim_to_budget()`
- `build_summary()` — 为被裁剪内容生成摘要（预留扩展）

#### P0-05：无 TodoWrite 动态规划 → ✅ 已修复

**修改** `agent/tools/registry.py`：
- 新增 `todowrite` 工具定义（OpenAI Function Calling schema）
- `dispatch_tool("todowrite")` → 写入 `state.todos`，重置 `rounds_since_todo`

**修改** `agent/agent_loop.py`：
- System Prompt 新增「任务规划规则」段落
- Nag 机制：`rounds_since_todo >= 3` 且有待办 → 自动注入提醒消息
- `_render_todo_section()` — 实时渲染待办状态到 nag 消息

#### P0-06：可观测性 → ✅ 已修复

**新增** `agent/harness/observ.py`：
- `Trace` 类：结构化事件记录（llm_call / tool_exec / nag / error）
- 每轮 LLM 调用自动记录 tokens_in / tokens_out / tool_calls
- 每次工具执行自动记录名称 / 成功 / 耗时 ms
- `trace.dump()` → `.traces/{session_id}.json` 持久化
- `trace.report()` → 终端输出执行摘要

#### P0-07：Harness 安全边界 → ✅ 已修复

**新增** `agent/tools/registry.py`：
- `TOOL_DEFINITIONS` — 工具白名单，10 个工具的完整 JSON Schema
- `dispatch_tool()` — 统一调度分发，带完整异常捕获 + traceback
- 所有工具执行通过 registry 路由，非白名单工具返回 error

**新增** `agent/state.py`：
- `@dataclass AgentState`：统一状态管理
- 包含 `todos`, `rounds_since_todo`, `total_tokens_in/out`, `round_count`
- 状态独立于 messages，不污染 LLM 上下文

### 架构变化

```
agent/
├── agent_loop.py          ← Harness 核心调度（重构）
├── state.py               ← 统一 AgentState（新增）
├── formatter.py
├── llm_client.py
├── memory/
│   ├── session.py         ← L1+L2 记忆（从旧 memory.py 迁移）
│   ├── persistence.py     ← L3 长期记忆（新增）
│   └── trim.py            ← Token 预算管理（新增）
├── tools/
│   └── registry.py        ← 工具白名单 + 调度（新增）
├── harness/
│   └── observ.py          ← 结构化 trace（新增）
└── workflow.py            ← 旧流水线（不再使用）
```

### 测试结果

- ✅ 降级模式：8 步流水线正常，L3 记忆自动保存，Trace 文件生成
- ✅ `.memory/index.json`：记录 `last_building=E座`, `last_participants=50`, `last_activity_type=讲座`
- ✅ `.traces/*.json`：结构化事件日志完整

### 文件变更

| 文件 | 操作 |
|------|------|
| `agent/state.py` | 新建 |
| `agent/memory/__init__.py` | 新建 |
| `agent/memory/session.py` | 从 agent/memory.py 迁移 |
| `agent/memory/persistence.py` | 新建 |
| `agent/memory/trim.py` | 新建 |
| `agent/tools/__init__.py` | 新建 |
| `agent/tools/registry.py` | 新建 |
| `agent/harness/__init__.py` | 新建 |
| `agent/harness/observ.py` | 新建 |
| `agent/agent_loop.py` | 重构 |

### 剩余 P0 问题

全部 7 项 P0 已修复 ✅：
- ~~P0-01~~ ✅ 重构为 Agent Loop
- ~~P0-02~~ ✅ 统一方案生成路径
- ~~P0-03~~ ✅ L3 长期记忆
- ~~P0-04~~ ✅ Token 预算管理
- ~~P0-05~~ ✅ TodoWrite 动态规划
- ~~P0-06~~ ✅ 结构化 Trace
- ~~P0-07~~ ✅ 工具白名单

---

## 2026-06-02 — Agent 架构全面诊断（对照六期教学 + Agent/Skill/Harness 三层模型）

> 诊断依据：四份学习材料（AI_agent.md / What_is_agent.md / agent.md / harness.md）  
> 诊断范围：Campus Compass 全部模块  
> 综合评分：**38 / 70**

---

### 总体评分

| 维度 | 得分 | 评价 |
|------|:--:|------|
| Agent Loop（闭环） | 7/10 | 有 Thought→Action→Observation 循环，缺 stop_reason 判断和死循环检测 |
| Memory（记忆） | 6/10 | L1/L2/L3 三层齐全，缺主动压缩（compaction）和 checkpoint 恢复 |
| Planning（规划） | 5/10 | TodoWrite 存在但约束太软，模型可绕过直接调 finalize |
| Skill（技能包） | 2/10 | 6 个 SKILL.md 文件存在但**完全未接入 Agent Loop** |
| Sub-Agent（子代理） | 2/10 | 只是函数调度器，无 LLM 驱动、无上下文隔离 |
| Harness（驾驭层） | 5/10 | 骨架齐全（loop+registry+trim+trace），缺硬护栏和安全边界 |
| Agent Team（团队） | 0/10 | 完全缺失 |

---

### 🔴 P0 级缺陷（影响可靠性，必须修）

#### P0-1：Skill 系统完全悬空 — 6 个 SKILL.md 是死代码

`agent/skill_loader.py` 能扫描和匹配 Skill，但 `agent_loop.py` 的 system prompt **从未注入匹配到的 Skill**。只有 `plan_generator.py` 做了 `SKILL_TO_CATEGORY` 的弱映射（只拿 category 标签，不拿 SOP 内容）。

**违背的原则**（agent.md 第四期）：Skill 的价值 = 降低随机性，沉淀方法论。Agent 策划"竞赛"和"讲座"用同一套流程，不会根据 Skill SOP 调整策略。

**修复方向**：Agent Loop 启动时调用 `match_skill()` → 注入 SOP 表到 system prompt 的 `## 当前技能指引` 段落；host_guides / materials 模板喂给 plan_generator。

---

#### P0-2：Sub-Agent 不是 Agent — 无 LLM 驱动、无上下文隔离

`agent/tools/subagent.py` 的 `classroom_scout` 和 `budget_analyst` 本质是 Python 函数——直接调 `query_rooms()` / `rank_rooms()` / `estimate_budget()`，没有任何 LLM 参与。

**违背的原则**（agent.md 第五期）：子代理的价值是"多一个干净上下文"，有自己的 `messages[]`，跑完只回传摘要。真正的子代理如果 `find_classrooms` 返回 0 间，会自己决定扩大搜索范围。

**修复方向**：重构为 `run_subagent(prompt, agent_type) → summary` 模式，子代理有自己的 messages + 白名单工具 + max_turns。

---

#### P0-3：无死循环检测（Circuit Breaker）

`MAX_TURNS=15` 存在，但**没有检测"同一工具同样参数连续调用"**。

**违背的原则**（agent.md 第三期）：模型在不确定时会反复绕圈（同一工具同样参数再调一次），必须设断路保护。

**修复方向**：连续 3 轮同一工具+同一参数 → 注入 `[Circuit Breaker] 你已重复调用此工具 3 次，请换一种方式或跳过此步。`

---

#### P0-4：Finalize 前置校验缺失 — 模型说 done ≠ 真的 done

TodoWrite 约束全靠 system prompt 软约束，LLM 可在 todo 列表有 `pending` 项时直接调 `finalize`。

**违背的原则**（agent.md 第四期坑②）：模型会"伪造更新"——只说"我更新了"但不调工具。Harness 必须代码级校验。

**修复方向**：在 `dispatch_tool("finalize")` 中拦截——如果还有 pending/in_progress 的 todo，返回 error 而非允许结束。

---

### 🟡 P1 级缺陷（影响稳定性，应该修）

#### P1-5：System Prompt 无「环境信息」注入

System prompt 纯静态，没有注入当前可用的教室数量/分布概况、建筑列表、学期阶段等环境信息。模型在"盲选"教室。

**违背的原则**（harness.md L2 层）：上下文架构应按需加载环境信息，而非让模型猜测。

---

#### P1-6：工具返回结果无 Token 截断

所有工具返回 `json.dumps(result)` 全量输出。如果 Tavily 返回 10 条长结果，单次 observation 可能占几千 token。

**违背的原则**（agent.md 第三期）：工具返回结果必须纳入预算，observation 也要截断。

---

#### P1-7：无 Post-Tool-Call 验证层

工具执行完就完了。`generate_activity_plan` 返回缺字段 JSON、`find_classrooms` 返回 0 间——都没有自动检测和修正建议。

**违背的原则**（harness.md 反馈闭环）：Agent writes → Harness 自动验证 → 若 fail 把 error 塞回 observation → Agent 修订。

---

#### P1-8：工具数量已到决策疲劳边界

12 个工具中，`parse_user_input` + `analyze_and_expand_topic` + `generate_activity_plan` 可合并为 `plan_activity` 复合工具；`find_classrooms` + `score_classrooms` 可合并为 `find_best_classroom`；`dispatch_subagent` 目前无实际价值。

**违背的原则**（harness.md 原则 2）：工具不是越多越好，每个 step 的 tool-choice 选错概率随工具数上升。

---

### 🟢 P2 级改进（远期方向）

| 编号 | 内容 | 对应教学 |
|------|------|----------|
| P2-9 | Agent Team 多角色协作（venue_scout / budget_analyst / content_planner + lead 调度） | agent.md 第六期 |
| P2-10 | 错误→规则学习管道（`.memory/known_issues.json` 积累踩坑经验） | harness.md 原则 3 |
| P2-11 | Checkpoint 快照（每 5 轮 dump state，支持崩溃恢复） | harness.md Checkpointing |

---

### 优先级路线图

```
Phase 1 ─ 可靠性（本周）
├── 🔴 P0-1: Skill 系统接入 Agent Loop ← 当前
├── 🔴 P0-2: 真正的 Sub-Agent（LLM 驱动 + 上下文隔离）
├── 🔴 P0-3: Circuit Breaker
├── 🔴 P0-4: Finalize 前置校验
└── 🟡 P1-5: System Prompt 环境信息注入

Phase 2 ─ 稳定性
├── 🟡 P1-6: Tool 结果 Token 截断
├── 🟡 P1-7: Post-Tool-Call 验证层
├── 🟡 P1-8: 工具合并精简
└── 🟢 P2-11: Checkpoint 快照

Phase 3 ─ 扩展性
├── 🟢 P2-9: Agent Team 多角色协作
└── 🟢 P2-10: 错误→规则学习管道
```
