# Campus Compass — 项目分工文档

> 版本：v1.0\
> 日期：2026-04-27\
> 技术栈：Python Flask + 原生 HTML/CSS/JS + SQLite + OpenAI API\
> 架构模式：单体应用 · 后端渲染 · 轻量 Web

***

## 一、项目结构总览

```
campus-compass/
│
├── data/                          # 📦 数据层
│   ├── init_db.py                 #   数据库初始化脚本
│   │                              #     - 建表（rooms 表）
│   │                              #     - 插入 7 间示例教室数据
│   │                              #     - 运行：python data/init_db.py
│   └── templates.json             #   活动模板（JSON 格式）
│                                  #     - 讲座 / 晚会 / 竞赛 / 社团活动
│                                  #     - 每种模板含：时间线、资源清单、预算、最低人数
│
├── engine/                        # 🧠 引擎层（Agent 核心大脑）
│   ├── __init__.py                #   空文件，标识为 Python 包
│   ├── workflow.py                #   主工作流（8 步骤，固定顺序执行）
│   ├── intent_parser.py           #   意图解析器
│   │                              #     - 从用户输入中提取活动类型、人数、建筑、设备
│   │                              #     - 关键词匹配 + 正则提取
│   ├── template_matcher.py        #   模板匹配器
│   │                              #     - 根据活动类型加载对应模板
│   │                              #     - 处理模板文件不存在时的兜底逻辑
│   └── room_scorer.py             #   教室打分排序算法
│                                  #     - 容量匹配度、楼层、设备匹配、建筑偏好
│                                  #     - 综合评分后降序排列
│
├── tools/                         # 🔧 工具层（纯 Python 计算，不依赖外部服务）
│   ├── __init__.py                #   空文件，标识为 Python 包
│   ├── db_service.py              #   数据库操作服务
│   │                              #     - query_rooms()：按条件查询教室
│   │                              #     - get_room_by_id()：按 ID 查询单间教室
│   ├── navigation.py              #   导航文本生成器
│   │                              #     - 根据教室信息生成步行导航指引文本
│   │                              #     - 包含建筑位置、楼层、参照物
│   └── budget_calc.py             #   预算计算器
│                                  #     - 根据模板和人数估算各项费用
│                                  #     - 人数越多费用按比例增加
│
├── web/                           # 🌐 Web 展示层
│   ├── app.py                     #   Flask 主程序
│   │                              #     - GET /     → 渲染聊天页面
│   │                              #     - POST /chat → 接收消息，返回 HTML 结果
│   └── templates/
│       └── index.html             #   单页聊天界面（930 行）
│                                  #     - 原生 HTML + CSS + JS
│                                  #     - 青春活力校园风格 UI
│                                  #     - 动态光晕背景 + 毛玻璃效果
│                                  #     - 快捷入口卡片 + 功能标签
│                                  #     - 时间线 / 预算网格 / 教室排名卡片 / 导航区
│
├── formatter.py                   # 📝 输出格式化器
│                                  #     - build_html()：生成 HTML 格式的策划书
│                                  #     - build_markdown()：生成 Markdown 格式（命令行用）
│                                  #     - 调用方：workflow.py 的 run_workflow()
│
├── llm_client.py                  # 🤖 LLM 客户端
│                                  #     - call_llm_for_ideation()：调用 OpenAI API 生成创意
│                                  #     - 提示词构建 + JSON 解析 + 兜底逻辑
│                                  #     - 仅在工作流 Step 2 调用一次
│
├── config.py                      # ⚙️ 配置管理
│                                  #     - 从环境变量 / .env 文件读取 API Key
│                                  #     - 数据库路径、模板路径常量定义
│
├── main.py                        # 🖥 命令行入口
│                                  #     - 初始化数据库 → 交互式循环
│                                  #     - 可在终端中直接测试（不启动 Web 服务）
│
├── requirements.txt               # 📋 Python 依赖清单
│   (flask, requests, python-dotenv)
│
└── .gitignore                     # 🙈 Git 忽略规则
    (venv/, __pycache__, .env, *.db, .DS_Store)
```

***

## 二、5 人分工方案

### 成员 1：Web 前端 + 展示层负责人

**角色定位**：负责用户看到和交互的一切，是项目的"门面"。

| 负责文件                       | 说明                        | 核心技能要求                                       |
| -------------------------- | ------------------------- | -------------------------------------------- |
| `web/templates/index.html` | 聊天界面 HTML/CSS/JS（约 930 行） | HTML5、CSS3（Flex/Grid/动画）、原生 JS（Fetch/DOM 操作） |
| `web/app.py`               | Flask 主程序（路由、请求处理）        | Python 基础、Flask 框架                           |
| `formatter.py`             | HTML 输出格式化（策划书结构化渲染）      | Python 字符串拼接、HTML 模板                         |

**具体任务清单**：

| 阶段      | 任务                                        | 预计工作量 |
| ------- | ----------------------------------------- | ----- |
| 第 1 周   | 确认 Flask 路由能正常渲染页面；理解现有 CSS 结构和 JS 逻辑     | 2 天   |
| 第 2 周   | 优化聊天界面的交互体验（加载动画、消息滚动、错误提示）               | 2 天   |
| 第 2-3 周 | 与 `formatter.py` 联调，确保后端返回的 HTML 在页面中正确渲染 | 2 天   |
| 第 3-4 周 | 响应式适配（手机端）、UI 细节打磨                        | 2 天   |
| 第 4 周   | 整体联调测试、修复 UI Bug                          | 1 天   |

**与谁对接**：

- 与 **成员 2**（workflow\.py）：通过 `web/app.py` → `run_workflow()` 调用
- 与 **成员 1 自己**（formatter.py）：HTML 输出格式与 CSS 样式对应

**交付物**：

- 完整可用的聊天界面
- 响应式布局（PC + 手机）
- 策划书在页面中以卡片形式美观展示

***

### 成员 2：引擎核心 + 工作流负责人

**角色定位**：负责 Agent 的核心大脑——工作流调度和意图理解。

| 负责文件                      | 说明                | 核心技能要求             |
| ------------------------- | ----------------- | ------------------ |
| `engine/workflow.py`      | 主工作流（8 步骤串联协调）    | Python 函数设计、模块调用   |
| `engine/intent_parser.py` | 意图解析器（关键词匹配 + 正则） | Python 字符串处理、正则表达式 |
| `engine/__init__.py`      | 包初始化文件            | Python 包管理基础       |

**具体任务清单**：

| 阶段      | 任务                                    | 预计工作量 |
| ------- | ------------------------------------- | ----- |
| 第 1 周   | 深入理解 8 步工作流的执行顺序和数据流转                 | 1 天   |
| 第 1-2 周 | 优化 `intent_parser.py`：扩充关键词词典、提高解析准确率 | 2 天   |
| 第 2 周   | 优化 `workflow.py`：增加错误处理、步骤日志、异常兜底     | 2 天   |
| 第 3 周   | 测试各种输入（正常/边界/乱码），确保工作流鲁棒              | 2 天   |
| 第 4 周   | 与所有模块联调，确保工作流串联正常                     | 1 天   |

**工作流步骤详解**：

```
Step 0: parse_intent(user_input)      → 意图解析（调用 intent_parser.py）
Step 1: match_template(intent)        → 模板匹配（调用 template_matcher.py）
Step 2: call_llm_for_ideation(intent) → 创意生成（调用 llm_client.py）
Step 3: _build_plan(...)              → 构建活动方案
Step 4: query_rooms(...)              → 查询教室（调用 db_service.py）
Step 5: rank_rooms(...)               → 教室打分排序（调用 room_scorer.py）
Step 6: generate_navigation(...)      → 生成导航（调用 navigation.py）
Step 7: estimate_budget(...)          → 计算预算（调用 budget_calc.py）
Step 8: build_html(...)               → 组装 HTML 输出（调用 formatter.py）
```

**与谁对接**：

- 调用 **成员 3** 的 `template_matcher.py` 和 `room_scorer.py`
- 调用 **成员 4** 的 `db_service.py`、`navigation.py`、`budget_calc.py`
- 调用 **成员 5** 的 `llm_client.py`
- 调用 **成员 1** 的 `formatter.py`
- 被 **成员 1** 的 `web/app.py` 调用

**交付物**：

- 完善的工作流（含错误处理和日志）
- 优化后的意图解析器
- 工作流测试报告

***

### 成员 3：模板匹配 + 教室推荐负责人

**角色定位**：负责活动模板管理和教室智能排序算法。

| 负责文件                         | 说明       | 核心技能要求                 |
| ---------------------------- | -------- | ---------------------- |
| `engine/template_matcher.py` | 模板匹配器    | Python 文件操作（JSON）、异常处理 |
| `engine/room_scorer.py`      | 教室打分排序算法 | Python 算法设计、多维加权评分     |
| `data/templates.json`        | 活动模板数据维护 | JSON 数据结构设计            |

**具体任务清单**：

| 阶段      | 任务                                    | 预计工作量 |
| ------- | ------------------------------------- | ----- |
| 第 1 周   | 熟悉 `templates.json` 的数据结构，理解模板匹配逻辑    | 1 天   |
| 第 1-2 周 | 丰富模板内容：扩充活动类型、优化时间线、调整默认预算            | 2 天   |
| 第 2 周   | 优化 `room_scorer.py` 的评分算法：调整权重、增加评分维度 | 2 天   |
| 第 3 周   | 准备演示用的真实校园教室数据（替换示例数据）                | 1 天   |
| 第 3-4 周 | 测试教室排序结果的合理性，调整评分参数                   | 2 天   |
| 第 4 周   | 与成员 2 联调，确保模板匹配和教室推荐结果正确              | 1 天   |

**教室评分算法维度**：

```
总分 = 容量匹配分 + 楼层分 + 设备匹配分 + 建筑偏好分

容量匹配:
  - 容量 ≥ 人数 → +10 分
  - 容量 ≥ 人数 + 10 → 额外 +5 分（留余量）

楼层:
  - 1-2 楼 → +5 分（低楼层优先）
  - 3-4 楼 → +2 分

设备匹配:
  - 每匹配一项用户需要的设备 → +3 分

建筑偏好:
  - 教室所在建筑匹配用户偏好 → +8 分
```

**与谁对接**：

- 被 **成员 2**（workflow\.py）调用
- `template_matcher.py` 读取 **成员 3 自己**维护的 `templates.json`
- `room_scorer.py` 接收 **成员 4**（db\_service.py）查出的教室列表

**交付物**：

- 完善的 4+ 套活动模板
- 优化后的教室评分算法
- 真实校园教室数据

***

### 成员 4：工具层 + 数据层负责人

**角色定位**：负责所有底层工具函数和数据库管理。

| 负责文件                   | 说明       | 核心技能要求          |
| ---------------------- | -------- | --------------- |
| `tools/db_service.py`  | 数据库操作服务  | Python + SQLite |
| `tools/navigation.py`  | 导航文本生成   | Python 字符串处理    |
| `tools/budget_calc.py` | 预算计算器    | Python 基础运算     |
| `data/init_db.py`      | 数据库初始化脚本 | SQLite DDL/DML  |
| `tools/__init__.py`    | 包初始化文件   | Python 包管理      |

**具体任务清单**：

| 阶段      | 任务                                        | 预计工作量 |
| ------- | ----------------------------------------- | ----- |
| 第 1 周   | 配置数据库：运行 `init_db.py`，验证数据正确性             | 1 天   |
| 第 1-2 周 | 收集并录入真实校园教室数据（建筑名、楼层、容量、设备、位置坐标等）         | 3 天   |
| 第 2 周   | 完善 `db_service.py`：增加更多查询条件（按设备筛选、按楼层筛选等） | 1 天   |
| 第 2-3 周 | 优化 `navigation.py`：根据真实校园布局编写更详细的导航文本     | 2 天   |
| 第 3 周   | 优化 `budget_calc.py`：根据活动类型细化费用标准          | 1 天   |
| 第 4 周   | 与成员 2 联调，确保工具层各函数被正确调用                    | 1 天   |

**数据库表结构**：

```sql
CREATE TABLE rooms (
    room_id       TEXT PRIMARY KEY,   -- 教室编号，如 "D101"
    building      TEXT NOT NULL,       -- 建筑名称，如 "思源书院D座"
    floor         INTEGER NOT NULL,    -- 楼层，如 1
    capacity      INTEGER NOT NULL,    -- 容纳人数，如 80
    area          REAL,                -- 面积（平方米）
    equipment     TEXT,                -- 设备清单（JSON 数组）
    entrance_note TEXT,                -- 入口指引
    nav_landmarks TEXT,                -- 导航参照物（JSON 数组）
    coordinate_x  INTEGER,            -- X 坐标
    coordinate_y  INTEGER             -- Y 坐标
);
```

**与谁对接**：

- 被 **成员 2**（workflow\.py）调用所有工具函数
- 数据库数据被 **成员 3**（room\_scorer.py）使用
- 数据库初始化脚本由 **成员 4** 自己维护

**交付物**：

- 真实校园教室数据库（≥10 间）
- 完整的导航文本
- 合理的预算计算标准

***

### 成员 5：LLM 客户端 + 配置 + 全栈串联负责人

**角色定位**：负责 AI 能力集成、系统配置、命令行入口和全栈模块间的串联协调。

| 负责文件               | 说明                | 核心技能要求                       |
| ------------------ | ----------------- | ---------------------------- |
| `llm_client.py`    | LLM API 调用封装      | Python + HTTP 请求（requests 库） |
| `config.py`        | 配置管理              | Python + 环境变量                |
| `main.py`          | 命令行入口             | Python 基础                    |
| `.env`             | 环境变量文件（API Key 等） | 文本编辑                         |
| `requirements.txt` | 依赖清单              | Python 包管理                   |
| `.gitignore`       | Git 忽略规则          | Git 基础                       |

**具体任务清单**：

| 阶段      | 任务                                   | 预计工作量 |
| ------- | ------------------------------------ | ----- |
| 第 1 周   | 注册 OpenAI 账号，获取 API Key；配置 `.env` 文件 | 1 天   |
| 第 1 周   | 测试 `llm_client.py` 的 API 调用是否正常      | 1 天   |
| 第 2 周   | 优化提示词（Prompt）设计：确保 LLM 返回结构化的 JSON   | 2 天   |
| 第 2 周   | 完善兜底逻辑：API 不可用时返回合理的默认方案             | 1 天   |
| 第 3 周   | 作为全栈串联者：协助前端与后端联调，解决集成问题             | 2 天   |
| 第 3-4 周 | 编写测试用例：覆盖各种输入场景                      | 2 天   |
| 第 4 周   | 编写项目文档、演示 PPT、准备答辩                   | 2 天   |

**LLM 调用时机**：

```
整个项目中，LLM 只在 1 个位置被调用：

engine/workflow.py → run_workflow()
    → Step 2: call_llm_for_ideation(intent)
        → llm_client.py

不在任何其他地方调用！这是"去 LLM 依赖重"架构的核心设计。
```

**提示词（Prompt）设计**：

```python
prompt = f'''
请根据以下信息生成校园活动方案:

活动类型: {activity_type}
预计人数: {participants}人
用户想法: {theme}

请用JSON格式输出（只输出JSON，不要其他文字）:
{{
    "recommended_title": "推荐活动标题",
    "activity_timeline": [
        {{"time": "14:00-14:30", "content": "签到入场"}},
        ...
    ],
    "resource_list": ["设备1", "设备2"],
    "promotion_suggestion": "宣传建议文字"
}}
'''
```

**兜底策略**（API 不可用时）：

```python
def _fallback_ideation(intent):
    return {
        "recommended_title": f"{intent['activity_type']}策划方案",
        "activity_timeline": [
            {"time": "14:00-14:30", "content": "签到入场"},
            {"time": "14:30-16:00", "content": "活动主体"},
            {"time": "16:00-16:30", "content": "互动与总结"},
        ],
        "resource_list": ["投影仪", "音响", "签到表"],
        "promotion_suggestion": "通过校园公众号和海报进行宣传",
    }
```

**与谁对接**：

- 被 **成员 2**（workflow\.py）在 Step 2 调用
- 被 **成员 1**（web/app.py 启动时）间接依赖
- 作为全栈串联者，与 **成员 1\~4** 全部有对接关系

**交付物**：

- 可用的 LLM 集成（含兜底）
- 优化的提示词
- `.env` 配置模板
- 项目文档和演示 PPT

***

## 三、协作关系图

```
┌──────────────────────────────────────────────────────────┐
│                                                          │
│   成员 1（Web 前端 + 展示层）                              │
│   ┌──────────────┐    ┌─────────────┐    ┌─────────────┐ │
│   │ web/app.py   │───→│ engine/     │───→│ formatter.py│ │
│   │ index.html   │    │ workflow.py │    │             │ │
│   └──────────────┘    └──────┬──────┘    └─────────────┘ │
│                              │ 调用                       │
│         ┌────────────────────┼────────────────────┐       │
│         │                    │                    │       │
│         ▼                    ▼                    ▼       │
│   成员 2（引擎核心）   成员 3（模板+教室）   成员 4（工具+数据）│
│   ┌──────────────┐   ┌──────────────┐   ┌──────────────┐  │
│   │ workflow.py  │   │ template_    │   │ db_service.py│  │
│   │ intent_      │   │   matcher.py │   │ navigation.py│  │
│   │   parser.py  │   │ room_scorer  │   │ budget_calc  │  │
│   └──────────────┘   │   .py        │   │   .py        │  │
│                      │ templates    │   │ init_db.py   │  │
│                      │   .json      │   └──────────────┘  │
│                      └──────────────┘                     │
│                              │                            │
│         ┌────────────────────┘                            │
│         ▼                                                 │
│   成员 5（LLM + 配置 + 全栈串联）                          │
│   ┌──────────────┐   ┌─────────┐   ┌──────────────┐      │
│   │ llm_client.py│   │config.py│   │ main.py       │      │
│   │ .env         │   │require- │   │ 文档 + PPT    │      │
│   │              │   │ments.txt│   │ 全栈协调      │      │
│   └──────────────┘   └─────────┘   └──────────────┘      │
│                                                          │
└──────────────────────────────────────────────────────────┘

依赖方向：上层调用下层
Web 层 → 引擎层 → 工具层 / LLM 客户端
```

***

## 四、分阶段开发计划

### 第 1 周：理解与准备（全员并行）

| 成员   | 任务                                                        | 输出            |
| ---- | --------------------------------------------------------- | ------------- |
| 成员 1 | 运行 Flask，理解 `index.html` 结构；理解 `formatter.py` 与 CSS 的对应关系 | 能修改 CSS 并看到效果 |
| 成员 2 | 理解 `workflow.py` 的 8 步流程；梳理数据在各步骤间的传递                     | 画出数据流图        |
| 成员 3 | 理解 `templates.json` 和 `room_scorer.py` 的评分逻辑              | 列出需要优化点       |
| 成员 4 | 运行 `init_db.py`；收集校园真实教室数据                                | 教室数据表（≥10 间）  |
| 成员 5 | 注册 OpenAI、获取 Key、测试 API 调用；理解 `.env` 配置                   | API 调用成功截图    |

### 第 2 周：核心优化（分组并行）

| 组别  | 成员       | 任务                       |
| --- | -------- | ------------------------ |
| 前端组 | 成员 1     | 优化 UI 交互：加载动画、消息滚动、移动端适配 |
| 引擎组 | 成员 2 + 3 | 优化意图解析 + 模板匹配 + 教室评分     |
| 工具组 | 成员 4 + 5 | 录入真实数据 + 优化 LLM 提示词      |

### 第 3 周：集成联调（全员协作）

| 成员   | 任务                      |
| ---- | ----------------------- |
| 成员 1 | 前端与后端联调，确保策划书 HTML 正常渲染 |
| 成员 2 | 工作流全面测试，处理边界情况          |
| 成员 3 | 教室推荐结果验证与调优             |
| 成员 4 | 导航文本与真实校园对照验证           |
| 成员 5 | 全栈串联，解决集成问题             |

### 第 4 周：收尾交付

| 成员   | 任务              |
| ---- | --------------- |
| 成员 1 | 最终 UI 打磨、Bug 修复 |
| 成员 2 | 最终工作流测试         |
| 成员 3 | 演示数据准备          |
| 成员 4 | 部署文档            |
| 成员 5 | 演示 PPT + 项目文档   |

***

## 五、开发规范

### 代码风格

- **Python**：遵循 PEP 8，使用 4 空格缩进
- **HTML/CSS**：使用 2 空格缩进
- **变量命名**：Python 使用 `snake_case`；JS 使用 `camelCase`
- **函数**：每个函数必须有明确的文档注释（docstring）

### Git 规范

- **分支策略**：
  - `main`：稳定分支（仅合并已验证的代码）
  - `dev-xxx`：个人开发分支（如 `dev-member1`、`dev-member2`）
- **提交信息格式**：`[模块] 简短描述`
  - 例：`[engine] 优化意图解析器关键词词典`
  - 例：`[web] 修复移动端消息气泡溢出`

### 测试规范

- 每完成一个模块，在终端运行 `python main.py` 进行命令行测试
- 每完成一个功能，启动 `python web/app.py` 在浏览器中验证
- 模拟非正常输入（空字符串、超长文本、特殊字符），确保系统不崩溃

***

## 六、常见问题与解决方案

| 问题         | 原因                    | 解决方法                                       |
| ---------- | --------------------- | ------------------------------------------ |
| 网页白屏       | Flask 未启动或端口冲突        | 检查终端：`python web/app.py` 是否正常              |
| LLM 调用失败   | API Key 未配置或无效        | 检查 `.env` 文件，确认 `LLM_API_KEY` 已设置          |
| 教室查不到结果    | 数据库未初始化               | 运行 `python data/init_db.py`                |
| 模板加载失败     | `templates.json` 路径不对 | 检查 `config.py` 中的 `TEMPLATE_PATH`          |
| 前后端联调报 404 | 路由不匹配                 | 确认 `web/app.py` 中 `@app.route("/chat")` 存在 |

***

## 七、签字确认

| 成员   | 姓名     | 负责模块            | 签字     | 日期     |
| ---- | ------ | --------------- | ------ | ------ |
| 成员 1 | <br /> | Web 前端 + 展示层    | <br /> | <br /> |
| 成员 2 | <br /> | 引擎核心 + 工作流      | <br /> | <br /> |
| 成员 3 | <br /> | 模板匹配 + 教室推荐     | <br /> | <br /> |
| 成员 4 | <br /> | 工具层 + 数据层       | <br /> | <br /> |
| 成员 5 | <br /> | LLM + 配置 + 全栈串联 | <br /> | <br /> |

***

<br />

<br />

