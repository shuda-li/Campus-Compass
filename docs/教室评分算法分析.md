# Campus Compass — 教室评分算法理论分析与优化方案

> 版本：v2.0  
> 日期：2026-04-27  
> 性质：理论探讨，暂不涉及代码修改  

---

## 一、当前算法实现现状

### 1.1 评分维度（4 维）

| 维度 | 分值范围 | 实现方式 | 权重占比 |
|------|:------:|---------|:------:|
| 容量适配度 | 0 ~ 15 分 | 按余量比例分档（刚好/最佳/偏大/过大） | ~30% |
| 楼层便利性 | 0 ~ 5 分 | 1-2楼 +5，3-4楼 +2，其他 0 | ~10% |
| 设备匹配度 | 0 ~ 15 分 | 每匹配一项 +3（子串模糊匹配） | ~30% |
| 建筑偏好 | 0 ~ 10 分 | 用户指定建筑时匹配 +10 | ~20% |

### 1.2 数据模型

```sql
rooms (
    room_id,           -- 教室编号
    building,          -- 建筑名
    floor,             -- 楼层
    capacity,          -- 额定容量（人）
    area,              -- 面积（m²）
    equipment,         -- 设备 JSON 数组
    entrance_note,     -- 入口指引文本
    nav_landmarks,     -- 导航参照物 JSON 数组
    coordinate_x,      -- 纯数字坐标（非真实地理坐标）
    coordinate_y       -- 纯数字坐标（非真实地理坐标）
)
```

### 1.3 关键局限速览

```
┌──────────────────────────────────────────────────────────────┐
│  维度            当前状态             缺陷                    │
├──────────────────────────────────────────────────────────────┤
│  容量适配度      4 档分梯           缺活动类型适配            │
│  地理位置        仅用楼层替代         缺真实地图和距离计算     │
│  设备匹配        子串模糊匹配         无设备优先级加权          │
│  建筑偏好        二元匹配（有/无）    无多建筑偏好支持          │
│  ❌ 时间冲突     未实现              完全缺失                 │
│  ❌ 可达性       未实现              无电梯/无障碍信息          │
│  ❌ 环境因素     未实现              无空调/噪音/采光考量      │
│  ❌ 历史数据     未实现              无使用率/故障率分析       │
│  ❌ 活动类型适配 未实现              不同活动需求不同          │
│  权重系统        硬编码固定值         不可配置，无差异化         │
└──────────────────────────────────────────────────────────────┘
```

---

## 二、维度一：人数容量余量 —— 深度分析

### 2.1 当前逻辑

```python
ratio = (capacity - participants) / participants

if ratio <= 0.05:   →  8 分   # 刚好
elif ratio <= 0.30: → 15 分   # 最佳
elif ratio <= 0.80: → 12 分   # 偏大
else:               →  5 分   # 过大
```

### 2.2 存在的问题

**问题 1：忽略人均面积**

容量（capacity）只计人数，但不同活动对空间密度的需求截然不同：

| 活动类型 | 人均面积需求 | 100 人所需面积 | 200 人教室可否？ |
|---------|:----------:|:------------:|:-------------:|
| 讲座（排座） | 0.8 m²/人 | 80 m² | ✅ 200 m² 浪费 |
| 竞赛（桌子+电脑） | 2.0 m²/人 | 200 m² | ✅ 刚好 |
| 晚会（站立+舞台） | 1.5 m²/人 | 150 m² | ✅ 但偏紧 |
| 社团沙龙（围圈） | 1.2 m²/人 | 120 m² | ⚠️ 稍大 |

当前算法只看 `capacity` 不看 `area`，导致：200 人报告厅对于 50 人讲座被扣分（ratio=300%，仅得 5 分），但如果用户有舞台需求，这个空间反而是合理的。

**优化建议**：

```
引入「活动类型 × 人均面积系数」:
  - 讲座:     min_area = participants × 0.8
  - 竞赛:     min_area = participants × 2.0
  - 晚会:     min_area = participants × 1.5
  - 社团活动:  min_area = participants × 1.2

容量评分 = f(capacity_ratio) × 0.6 + f(area_ratio) × 0.4
```

**问题 2：梯度跳跃过大**

同一档内所有教室得分相同，无法细分：

```
50 人活动，余量 6% 和 29% 都是 15 分（最佳档）
→ 实际差异 23% 被抹平
```

**优化建议**：

```
采用连续函数替代分档:

score = max(0, 15 × (1 - |ratio - 0.15| / 0.5))

        15 ┤          ╱‾‾‾╲
           │         ╱      ╲
        10 ┤        ╱        ╲
           │       ╱          ╲
         5 ┤      ╱            ╲
           │     ╱              ╲
         0 ┤────╱                ╲────
           └──────────────────────────
              0%  15%  30%    80%    ratio

峰值在 15%（最佳余量），两侧平滑递减
```

**问题 3：无"弹性容量"概念**

部分教室可通过增减桌椅调节容量，当前 `capacity` 是硬编码的额定值：

**优化建议**：

```
rooms 表增加字段:
  - capacity_max: 最大容量（加临时座位后）
  - capacity_min: 最低教学容量

评分时考虑弹性区间:
  if participants <= capacity_max:
      score += bonus_for_flexibility
```

---

## 三、维度二：地理位置便利性 —— 深度分析

### 3.1 当前逻辑

当前仅用 `floor`（楼层）作为地理位置便利性的唯一指标——**这远不足以代表真实的地理便利性**。

两个同为 2 楼的教室，一个在教学区中心，一个在边缘，当前算法无法区分。

### 3.2 地理便利性的理想评估维度

```
地理总评分 = Σ (到达概率 × 通勤成本)

通勤成本 = f(距离, 路径质量, 换乘次数, 天气影响)
```

应至少考虑以下起点（Origin）：

| 起点类型 | 示例 | 权重说明 |
|---------|------|---------|
| 主要教学区 | 教学楼群核心广场 | 大部分参与者从这里出发 |
| 宿舍区 | 学生宿舍楼群 | 晚间活动的出发地 |
| 校门/公交站 | 校外参与者入口 | 对外活动的关键入口 |
| 食堂/生活区 | 餐饮中心 | 午间活动的参照点 |

### 3.3 真实地图数据方案

**方案 A：高德/百度地图 API（推荐）**

```
数据获取:
  - 步行路径规划 API  → 获取步行距离 + 预估时间
  - 地理编码 API      → 将建筑名转为经纬度

评分计算:
  for origin in origins:                          # 遍历所有起点
      route = amap.walking(origin, destination)   # API 获取路径
      distance_score  = 10 - min(distance/100, 10)  # 每 100m 减 1 分
      time_score      = 10 - min(time/5, 10)        # 每 5min 减 1 分
      path_score      = score_path_quality(route)    # 路径质量评分
      total += weight × (distance_score + time_score + path_score) / 3

优点: 数据准确实时，支持路况、施工绕行
缺点: 需要 API Key，有网络依赖
```

**方案 B：静态校园图数据（离线可用）**

如果无法接入实时地图 API，可在 `rooms` 表中扩充数据：

```sql
-- 升级后的 rooms 表地理字段
ALTER TABLE rooms ADD COLUMN latitude REAL;          -- 真实纬度
ALTER TABLE rooms ADD COLUMN longitude REAL;         -- 真实经度
ALTER TABLE rooms ADD COLUMN distance_to_teaching REAL;  -- 距教学区核心距离(m)
ALTER TABLE rooms ADD COLUMN distance_to_dorm REAL;      -- 距宿舍区距离(m)
ALTER TABLE rooms ADD COLUMN distance_to_gate REAL;      -- 距校门距离(m)
ALTER TABLE rooms ADD COLUMN has_elevator INTEGER;       -- 有无电梯 (0/1)
ALTER TABLE rooms ADD COLUMN path_quality TEXT;          -- 路径描述 ("直路"/"需穿楼"/"需爬坡")
```

**路径质量评分建议**：

| 路径特征 | 加分/扣分 | 理由 |
|---------|:-------:|------|
| 全程室内走廊 | +3 | 不受天气影响 |
| 需穿越室外 | 0 | 正常 |
| 需爬坡/台阶 | -2 | 不便，尤其搬运设备 |
| 沿途有施工 | -3（临时） | 安全风险 |
| 夜间有路灯 | +1 | 晚间活动安全 |
| 有自行车道 | +1 | 多出行方式 |

### 3.4 基于坐标的简易距离计算（当前可立刻实现）

当前已有 `coordinate_x` 和 `coordinate_y`，可定义几个"锚点"并计算欧几里得距离：

```python
# 定义校园关键锚点 (坐标可校准)
ANCHORS = {
    "教学楼核心":  (150, 100),   # 教学区中心
    "宿舍区核心":  (250, 180),   # 宿舍区中心
    "校门":       (50, 80),     # 主校门
    "食堂":       (180, 120),   # 餐饮中心
}

def distance_score(room):
    dx = room["coordinate_x"] - anchor_x
    dy = room["coordinate_y"] - anchor_y
    dist = math.sqrt(dx**2 + dy**2)
    return max(0, 10 - dist * 0.05)  # 每 20 单位减 1 分
```

这个方案不需要外部 API，但精度取决于坐标系的校准质量。

---

## 四、缺失维度的补充建议

### 4.1 时间冲突检测 （优先级：⭐⭐⭐⭐⭐）

当前算法完全不考虑教室是否已被占用。

**方案**：增加 `bookings` 表：

```sql
CREATE TABLE bookings (
    booking_id   INTEGER PRIMARY KEY,
    room_id      TEXT,
    event_name   TEXT,
    start_time   TEXT,    -- ISO 格式: "2026-05-10T14:00"
    end_time     TEXT,
    booked_by    TEXT
);
```

评分逻辑：

```
if room_has_conflict(room, target_time):
    score -= 999    # 直接排除
```

### 4.2 活动类型适配 （优先级：⭐⭐⭐⭐）

**核心洞察**：不同活动对教室有不同侧重，权重不应该固定。

```
┌──────────┬────────┬────────┬────────┬────────┬──────────┐
│ 活动类型  │ 容量   │ 位置   │ 设备   │ 面积   │ 环境     │
├──────────┼────────┼────────┼────────┼────────┼──────────┤
│ 讲座     │ ⭐⭐⭐⭐ │ ⭐⭐⭐  │ ⭐⭐⭐  │ ⭐⭐   │ ⭐⭐     │
│ 晚会     │ ⭐⭐⭐  │ ⭐⭐   │ ⭐⭐⭐⭐⭐│ ⭐⭐⭐⭐│ ⭐⭐⭐   │
│ 竞赛     │ ⭐⭐⭐  │ ⭐⭐   │ ⭐⭐⭐  │ ⭐⭐⭐⭐│ ⭐⭐⭐⭐⭐ │
│ 社团活动  │ ⭐⭐   │ ⭐⭐⭐⭐│ ⭐⭐   │ ⭐⭐⭐  │ ⭐⭐⭐   │
└──────────┴────────┴────────┴────────┴────────┴──────────┘

讲座: 核心是人多，只要有投影音响，位置适当即可
晚会: 设备最重要（舞台、灯光、音响），面积要够大
竞赛: 环境最重要（安静、独立），面积要够放设备
社团: 位置最重要（方便成员到达），设备要求低
```

**方案**：权重配置化

```python
ACTIVITY_WEIGHTS = {
    "讲座":    {"capacity": 0.40, "location": 0.25, "equipment": 0.25, "area": 0.10},
    "晚会":    {"capacity": 0.20, "location": 0.15, "equipment": 0.40, "area": 0.25},
    "竞赛":    {"capacity": 0.20, "location": 0.15, "equipment": 0.20, "area": 0.25, "environment": 0.20},
    "社团活动": {"capacity": 0.15, "location": 0.40, "equipment": 0.15, "area": 0.15, "environment": 0.15},
}
```

### 4.3 无障碍设施 （优先级：⭐⭐⭐）

对于有特殊需求的参与者（如轮椅使用者）：

```sql
ALTER TABLE rooms ADD COLUMN wheelchair_accessible INTEGER DEFAULT 0;
ALTER TABLE rooms ADD COLUMN nearest_elevator TEXT;     -- "东侧电梯"
ALTER TABLE rooms ADD COLUMN has_ramp INTEGER DEFAULT 0;
```

### 4.4 环境因素 （优先级：⭐⭐⭐）

```sql
ALTER TABLE rooms ADD COLUMN has_ac INTEGER DEFAULT 1;        -- 空调
ALTER TABLE rooms ADD COLUMN noise_level TEXT;                -- "安静"/"一般"/"嘈杂"
ALTER TABLE rooms ADD COLUMN natural_light TEXT;              -- "充足"/"一般"/"需灯光"
ALTER TABLE rooms ADD COLUMN seating_type TEXT;               -- "固定排椅"/"可移动桌椅"/"阶梯"
```

环境评分示例：

```python
def environment_score(room, activity_type, season):
    s = 0
    if room["has_ac"]:
        s += 3  # 空调 → 夏冬必需
    if room["noise_level"] == "安静":
        s += 3  # 讲座、竞赛需要安静
    if activity_type == "竞赛" and room["seating_type"] == "可移动桌椅":
        s += 4  # 竞赛需要灵活布局
    return s
```

### 4.5 历史使用数据 （优先级：⭐⭐）

加入使用反馈循环：

```sql
CREATE TABLE room_feedback (
    feedback_id  INTEGER PRIMARY KEY,
    room_id      TEXT,
    rating       INTEGER,       -- 1~5 星
    comment      TEXT,
    created_at   TEXT
);
```

长期来看，高评分教室获得额外加分，形成良性反馈循环。

---

## 五、权重系统优化

### 5.1 当前问题

- 所有维度硬编码固定权重
- 不同活动类型无法差异化
- 权重值缺乏理论依据

### 5.2 推荐的权重体系（分层可配置）

```
第一层：全局默认权重（适用未指定活动类型时）
第二层：活动类型权重（覆盖全局，针对性强）
第三层：用户偏好权重（用户明确指定的偏好，最高优先级）

最终权重 = 全局权重 × (1-α) + 活动权重 × α
        其中 α 随活动类型置信度动态调整
```

### 5.3 归一化建议

当前各维度分值范围不一致（5~15），直接用原始分相加会导致隐性权重偏差：

```python
# 归一化到 0~1 区间
normalized = (raw_score - min_val) / (max_val - min_val)

# 加权求和
final = Σ (normalized_i × weight_i)

# 总分映射回 0~100 便于理解
final_score = final × 100
```

---

## 六、计算效率分析

### 6.1 当前瓶颈

| 操作 | 时间复杂度 | 说明 |
|------|:--------:|------|
| SQL 查询 | O(n) | 全表扫描（无索引外） |
| 评分计算 | O(n × m) | n 间教室 × m 个评分维度 |
| 排序 | O(n log n) | Python 内置排序 |

对于校园场景（通常 < 500 间教室），当前复杂度完全可接受。

### 6.2 如需引入地图 API 的效率考量

如果接入高德/百度地图 API 做实时路径计算：

```
问题: 每间候选教室都需要 N 次 API 调用（N = 起点数）
      10 间教室 × 3 个起点 = 30 次 API 调用
      → 每次 200ms = 6 秒总延迟（不可接受）

方案 1: 预计算缓存
  - 系统初始化时，预计算所有教室到所有锚点的距离
  - 存入 rooms 表的 distance_* 字段
  - 查询时 O(1) 读取 → 零延迟

方案 2: 异步 + 渐进展示
  - 先用缓存数据快速出 TOP 5
  - 后台异步请求精确路径
  - 精确结果到达后更新展示

方案 3: 向量化计算
  - 批量请求：一次 API 调用传多个起点/终点
  - 高德批量路径规划 API：减少请求次数
```

### 6.3 性能优化建议（不引入外部 API 的前提下）

```python
# 1. 评分计算前做粗筛：
#    先用 SQL 直接过滤 (capacity, floor, building)
#    缩小候选集后再精细评分

# 2. 懒加载设备解析：
#    在 SQL 查询时直接用 json_extract 过滤
#    SELECT * FROM rooms WHERE json_extract(equipment, '$[0]') IS NOT NULL

# 3. 内存缓存：
#    教室静态数据（设备、面积、坐标）缓存在内存
#    仅动态数据（预订状态）每次查询
```

---

## 七、推荐的渐进式优化路线

### 阶段 1：立即可行（本周，不改动数据库结构）

1. ✅ 容量评分改用连续函数，替代分档跳变
2. ✅ 楼层评分置换为基于 coordinate_x/y 的距离计算（定义 3-4 个校园锚点）
3. ✅ 设备匹配从子串改为精确匹配 + 优先级加权（舞台>音响>投影）
4. ✅ 权重根据活动类型差异化（4 套模板对应 4 套权重）

### 阶段 2：短期优化（需小幅改表）

5. 🔲 rooms 表增加 `has_elevator`、`seating_type`、`has_ac` 列
6. 🔲 新增 bookings 表，实现时间冲突检测
7. 🔲 评分归一化到 0~100

### 阶段 3：中期优化（需外部集成）

8. 🔲 接入高德地图 API，预计算教室到锚点步行距离
9. 🔲 新增 room_feedback 表，收集使用评价
10. 🔲 环境因素评分（噪音、采光、温控）

### 阶段 4：远期（数据驱动）

11. 🔲 基于历史预订数据训练权重（机器学习微调）
12. 🔲 个性化推荐（根据用户所在学院/宿舍偏好）

---

## 八、总结

| 评估项 | 当前状态 | 优化空间 |
|--------|:------:|:------:|
| 维度全面性 | ⭐⭐ (4/9 维) | 缺时间冲突、可达性、环境、活动类型适配 |
| 权重合理性 | ⭐⭐ (固定硬编码) | 需活动类型差异化 + 归一化 |
| 地图数据准确性 | ⭐ (伪坐标) | 需真实经纬度或预计算距离 |
| 地图数据实时性 | N/A | 静态数据即可，不要求实时 |
| 计算效率 | ⭐⭐⭐⭐ (毫秒级) | 加入地图 API 后需缓存优化 |
| 可扩展性 | ⭐⭐⭐ (模块化) | 权重配置化后可达 ⭐⭐⭐⭐ |

**核心结论**：当前算法适合课堂演示场景（数据量小、无外部依赖），但要在真实校园环境中部署，最关键的改进是 **(1) 引入距离计算替代楼层评分** 和 **(2) 权重按活动类型差异化**。这两项改进不需要第三方 API，仅需丰富数据库字段 + 调整评分逻辑，即可显著提升推荐质量。
