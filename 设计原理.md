# Campus Compass — Agent 设计原理

> 版本：v1.0  
> 日期：2026-04-27  
> 技术栈：Python Flask + 原生 HTML/CSS/JS + SQLite + OpenAI API  

---

## 一、核心设计思想

### 1.1 一句话概括

> **Campus Compass 不是"和 AI 聊天"，而是我们自主设计的 8 步确定性引擎 + LLM 作为创意辅助插件。**

### 1.2 传统 Agent vs 我们的 Agent

| 对比维度 | 传统 LLM Agent | Campus Compass |
|---------|---------------|----------------|
| **决策方式** | LLM 自由发挥，黑盒决策 | 确定性引擎按固定流程调度 |
| **LLM 调用次数** | 多次（思维链、多轮对话） | **仅 1 次**（仅用于创意生成） |
| **数据准确性** | LLM 可能编造教室信息 | 数据库 SQL 查询，100% 真实 |
| **可解释性** | 黑盒，难以解释每一步 | 每步打印日志，完全可追溯 |
| **容错能力** | LLM 挂了系统全挂 | LLM 挂了自动用模板兜底，系统不崩溃 |
| **响应速度** | 慢（多轮 LLM 调用累加延迟） | 快（仅 1 次 API 调用，其余纯计算） |
| **运行成本** | 高（每次请求多次 API 调用） | 低（每次请求仅 1 次 API 调用） |
| **课程适配** | 看起来像"套壳 ChatGPT" | 体现自主设计的 Agent 架构 |

### 1.3 架构反转

```
❌ 传统做法：
   用户输入 → LLM 黑盒决策 → LLM 输出（LLM 说了算，不可控）

✅ 我们的做法：
   用户输入 → 确定性引擎调度（8 步固定流程）
                │
                ├─ Step 0-1: 规则引擎（意图解析 + 模板匹配）
                ├─ Step 2:   LLM 辅助（仅创意生成，1 次调用）
                ├─ Step 4-5: 算法决策（数据库查询 + 多维打分）
                └─ Step 6-7: 纯计算（导航文本 + 预算）
```

**核心原则：LLM 是"增强器"，不是"决策者"。**

---

## 二、8 步工作流详解

### 总览图

```
用户输入: "我想办一个50人的技术讲座，需要投影，最好在D座"
   │
   ▼
┌─────────────────────────────────────────────────────────┐
│                                                         │
│  Step 0   意图解析      规则引擎   提取: 讲座/50人/D座/投影  │
│     │                                                   │
│     ▼                                                   │
│  Step 1   模板匹配      知识库     加载"讲座"模板          │
│     │                                                   │
│     ▼                                                   │
│  Step 2   LLM 创意      OpenAI    生成标题+时间线+建议    │
│     │                                                   │
│     ▼                                                   │
│  Step 3   方案合成      合并逻辑   LLM优先，模板兜底       │
│     │                                                   │
│     ▼                                                   │
│  Step 4   查询教室      SQLite    WHERE capacity >= 50   │
│     │                                                   │
│     ▼                                                   │
│  Step 5   打分排序      评分算法   多维度加权 → 推荐D206   │
│     │                                                   │
│     ▼                                                   │
│  Step 6   生成导航      文本拼接   建筑→楼层→具体位置       │
│     │                                                   │
│     ▼                                                   │
│  Step 7   计算预算      费用模型   人数 × 费率系数          │
│     │                                                   │
│     ▼                                                   │
│  Step 8   组装HTML      格式化器   生成策划书网页           │
│                                                         │
└─────────────────────────────────────────────────────────┘
```

---

### Step 0：意图解析（`engine/intent_parser.py`）

**原理**：规则引擎，不用 LLM。

| 提取目标 | 方法 | 示例输入 | 提取结果 |
|---------|------|---------|---------|
| 活动类型 | 关键词字典匹配 | "讲座" → `ACTIVITY_KEYWORDS` | `activity_type = "讲座"` |
| 参与人数 | 正则表达式 | `(\d+)\s*人` | `participants = 50` |
| 偏好建筑 | 正则表达式 | `([A-Za-z]+座)` | `building = "D座"` |
| 设备需求 | 关键词列表 | `["投影","音响","灯光"...]` | `equipment = ["投影"]` |

```python
# 关键词词典（27个中文 + 4个英文关键词，覆盖4种活动类型）
ACTIVITY_KEYWORDS = {
    "讲座": ["讲座","演讲","分享","学术","报告","seminar","交流会","研讨会"],
    "晚会": ["晚会","演出","文艺","才艺","迎新","毕业","联欢","庆典","party"],
    "竞赛": ["竞赛","比赛","大赛","挑战赛","编程","辩论","答辩","contest"],
    "社团活动": ["社团","团建","聚会","沙龙","工作坊","workshop","见面会"],
}
```

**为什么不用 LLM 做意图解析？**
- 规则引擎 100% 可控：同样的输入永远得到同样的结果
- 零延迟：正则匹配 < 1ms，LLM 调用 > 1000ms
- 零成本：不需要消耗 API 额度
- 鲁棒性强：不受 LLM 随机性影响

---

### Step 1：模板匹配（`engine/template_matcher.py`）

**原理**：根据 Step 0 解析出的活动类型，从 `templates.json` 加载预定义模板。

```json
// data/templates.json 节选
{
  "讲座": {
    "activity_type": "讲座",
    "default_timeline": [
      {"time": "14:00-14:30", "content": "签到入场"},
      {"time": "14:30-14:45", "content": "主持人开场"},
      {"time": "14:45-16:00", "content": "嘉宾演讲 / 主题分享"},
      {"time": "16:00-16:30", "content": "问答互动环节"},
      {"time": "16:30-17:00", "content": "自由交流与结束"}
    ],
    "default_resources": ["投影仪","音响","麦克风","签到表"],
    "default_budget": {
      "场地布置": 200, "宣传物料": 150, "嘉宾礼品": 100, "茶歇饮料": 50
    },
    "minimum_capacity": 50
  }
}
```

**模板的定位**：领域知识库 + 保底方案。

- LLM 可用时：模板提供基础框架，LLM 在其上增强
- LLM 不可用时：模板直接作为唯一方案，系统仍能正常运行

---

### Step 2：LLM 创意生成（`llm_client.py`）← 唯一LLM调用

**原理**：将 Step 0 的结构化意图构造为提示词（Prompt），调用 OpenAI API，解析返回的 JSON。

```
                    提示词构造
                        │
   intent ──────────────────────→ prompt
   {                              │
     activity_type: "讲座",       ▼
     participants: 50,     ┌─────────────┐
     theme: "..."          │ OpenAI API  │
   }                       └──────┬──────┘
                                  │
                                  ▼
                          JSON 响应
                          {
                            "recommended_title": "前沿技术分享沙龙",
                            "activity_timeline": [...],
                            "resource_list": [...],
                            "promotion_suggestion": "..."
                          }
```

**提示词（Prompt）设计**：

```python
prompt = f'''
请根据以下信息生成校园活动方案:

活动类型: {activity_type}
预计人数: {participants}人
用户想法: {theme}

请用JSON格式输出（只输出JSON，不要其他文字）:
{{
    "recommended_title": "推荐活动标题",
    "activity_timeline": [
        {{"time": "14:00-14:30", "content": "签到入场"}},
        ...
    ],
    "resource_list": ["设备1", "设备2"],
    "promotion_suggestion": "宣传建议文字"
}}
'''
```

**提示词设计要点**：
1. **角色设定**：`"你是校园活动策划专家"` 限定输出领域
2. **结构化约束**：要求以 JSON 格式输出 → 易于程序解析
3. **格式示例**：给出期望的 JSON 结构 → 提高输出稳定性
4. **temperature = 0.8**：适度增加创造性，但不过分随机

**兜底策略**：LLM 不可用（API Key 未配、网络超时、返回异常）时自动降级为预设方案：

```python
def _fallback_ideation(intent: dict) -> dict:
    return {
        "recommended_title": f"{intent['activity_type']}策划方案",
        "activity_timeline": [
            {"time": "14:00-14:30", "content": "签到入场"},
            {"time": "14:30-16:00", "content": "活动主体"},
            {"time": "16:00-16:30", "content": "互动与总结"},
        ],
        "resource_list": ["投影仪", "音响", "签到表"],
        "promotion_suggestion": "通过校园公众号和海报进行宣传",
    }
```

---

### Step 3：合成活动方案（`workflow._build_plan`）

**原理**：优先级合并策略——LLM 输出优先，模板兜底。

```python
def _build_plan(intent, template, ideas):
    return {
        "title":     ideas.get("recommended_title")        # LLM 标题
                     or f"{intent['activity_type']}策划方案", # 兜底
        "timeline":  ideas.get("activity_timeline")        # LLM 时间线
                     or template.get("default_timeline"),   # 模板兜底
        "resources": ideas.get("resource_list")            # LLM 资源
                     or template.get("default_resources"),  # 模板兜底
        "promotion": ideas.get("promotion_suggestion",""),  # LLM 建议
    }
```

**核心逻辑**：`LLM 输出 or 模板数据`。只要 LLM 返回了有效字段就用 LLM 的，否则退回到模板的预设数据。

---

### Step 4：查询教室（`tools/db_service.py`）

**原理**：SQL 数据库查询，而非 LLM 推荐。

```sql
-- 用户需要容纳 50 人
SELECT * FROM rooms WHERE capacity >= 50;

-- 结果来自真实数据库 data/rooms.db：
┌─────────┬──────────────┬───────┬──────────┬──────────────────────┐
│ room_id │ building     │ floor │ capacity │ equipment            │
├─────────┼──────────────┼───────┼──────────┼──────────────────────┤
│ D101    │ 思源书院D座  │   1   │    80    │ 投影仪,音响,空调      │
│ D206    │ 思源书院D座  │   2   │    60    │ 投影仪,黑板          │
│ E301    │ 知行书院E座  │   3   │   120    │ 投影仪,音响,舞台,空调 │
│ E102    │ 知行书院E座  │   1   │    50    │ 投影仪,音响          │
│ F201    │ 致远楼F座    │   2   │    90    │ 投影仪,音响,空调      │
│ F403    │ 致远楼F座    │   4   │   200    │ 投影仪,音响,舞台,灯光 │
└─────────┴──────────────┴───────┴──────────┴──────────────────────┘
```

**为什么不让 LLM 推荐教室？**
- LLM 不知道你的校园有哪些教室
- LLM 可能编造不存在的教室编号
- 数据库查询 = 100% 准确 + 零延迟

---

### Step 5：教室打分排序（`engine/room_scorer.py`）

**原理**：多维加权评分算法。向用户输入 50 人 + 要投影 + 偏好 D 座：

```
评分公式: score = 容量分 + 楼层分 + 设备分 + 偏好分

┌──────────┬────────┬──────────┬────────┬────────┬──────┬─────┐
│ 教室      │ 容量   │ 容量分   │ 楼层分 │ 设备分 │ 偏好分│ 总分│
│          │        │ (+10/+5) │ (+5/+2)│ (+3×N) │ (+8)  │     │
├──────────┼────────┼──────────┼────────┼────────┼──────┼─────┤
│ D206(60) │ >=50✓  │ 10       │ 5(2F)  │ 3×1=3  │ 8(D)  │ 26  │
│ D101(80) │ >=50✓  │ 10       │ 5(1F)  │ 3×1=3  │ 8(D)  │ 26  │
│ E102(50) │ >=50✓  │ 10       │ 5(1F)  │ 3×1=3  │ 0     │ 18  │
│ F201(90) │ >=50✓  │ 10       │ 5(2F)  │ 3×1=3  │ 0     │ 18  │
│ E301(120)│ >=50✓  │ 10       │ 2(3F)  │ 3×2=6  │ 0     │ 18  │
│ F403(200)│ >=50✓  │ 10       │ 2(4F)  │ 3×2=6  │ 0     │ 18  │
└──────────┴────────┴──────────┴────────┴────────┴──────┴─────┘

最终排序: D206 (26分) = D101 (26分) > E102 = F201 = E301 = F403 (18分)
          ↑ 同分时 D206 优先（容量 60 更接近 50，避免浪费）
```

**评分维度设计原则**：

| 维度 | 分值范围 | 设计理由 |
|------|---------|---------|
| 容量匹配 | 0 ~ 15 分 | 最核心需求——教室必须能装下人 |
| 楼层 | 0 ~ 5 分 | 低楼层更方便（搬运设备、人员进出） |
| 设备匹配 | 0 ~ n×3 分 | 每多匹配一项设备加 3 分 |
| 建筑偏好 | 0 ~ 8 分 | 用户指定建筑时给予明显加分 |

---

### Step 6：生成导航（`tools/navigation.py`）

**原理**：纯文本拼接，基于数据库中真实存储的导航数据。

```python
def generate_navigation(room):
    return f"""
📍 目标教室: **{room_id}**（{building}）

🚶 导航路线:
   {entrance_note}                      # 例: "上楼后左转，走廊东侧第二个门"
   沿途参照物: {landmark1 → landmark2 → room_id}
   教室坐标: ({x}, {y})
"""
```

**数据来源**：`rooms` 表中的 `entrance_note` 和 `nav_landmarks` 字段，都是提前录入的真实校园指引。

---

### Step 7：计算预算（`tools/budget_calc.py`）

**原理**：模板基础费用 × 人数倍数系数。

```python
multiplier = 1.0
if participants > 100:  multiplier = 1.5
elif participants > 50:  multiplier = 1.2

# 讲座模板：{"场地布置": 200, "宣传物料": 150, "嘉宾礼品": 100, "茶歇": 50}
# 50 人: multiplier = 1.2
# 结果: {"场地布置": 240, "宣传物料": 180, "嘉宾礼品": 120, "茶歇": 60, "合计": 600}
```

---

### Step 8：组装 HTML 输出（`formatter.py`）

**原理**：将前面 7 步的结果组装成带 CSS class 的 HTML 片段。

```
plan (活动方案)  ─┐
rooms (教室推荐)  ─┤
navigation (导航)  ─┼──→ formatter.build_html() ──→ HTML 策划书
budget (预算)     ─┘
```

生成的 HTML 结构：

```html
<div class="plan-result">
  <div class="plan-hero">          ← 策划书头部（渐变背景 + 标题）
    <div class="plan-hero-icon">📋</div>
    <div class="plan-hero-text">
      <h2>前沿技术分享沙龙</h2>
      <span>...</span>
    </div>
  </div>
  <div class="plan-body">
    <div class="plan-section">     ← 时间线（左侧彩色竖线）
      <ul class="timeline-list">...</ul>
    </div>
    <div class="plan-section">     ← 预算（2 列网格）
      <div class="budget-grid">...</div>
    </div>
    <div class="plan-section">     ← 教室推荐（排名卡片）
      <div class="room-cards">...</div>
    </div>
    <div class="plan-section">     ← 导航指引（绿色背景）
      <div class="nav-guide">...</div>
    </div>
    <div class="plan-section">     ← 资源清单
      <div class="budget-grid">...</div>
    </div>
  </div>
</div>
```

这个 HTML 片段通过 AJAX 返回浏览器，直接插入到聊天区域。前端 CSS 已经预定义了所有 class 的样式（时间线的竖线、卡片的排名徽章等）。

---

## 三、LLM 的精确角色定位

### 3.1 LLM 负责什么

| 任务 | 说明 |
|------|------|
| 🎨 活动标题 | 如 "前沿技术分享沙龙"（模板只有 "讲座策划方案"） |
| 📅 个性化时间线 | 如 "14:00 签到 → 14:30 AI 前沿演讲 → 15:30 实践 Workshop" |
| 📝 宣传建议 | 如 "建议在计算机学院公众号和食堂海报栏同步宣传" |
| 🛠 资源清单 | 补充模板中没有的设备（如 "录播设备"、"茶歇台"） |

### 3.2 LLM 不负责什么

| 不做的任务 | 实际由谁做 |
|-----------|-----------|
| ❌ 识别活动类型 | `intent_parser.py`（关键词匹配） |
| ❌ 提取参与人数 | `intent_parser.py`（正则表达式） |
| ❌ 选择活动模板 | `template_matcher.py`（JSON 加载） |
| ❌ 查询可用教室 | `db_service.py`（SQL 查询） |
| ❌ 推荐最佳教室 | `room_scorer.py`（评分算法） |
| ❌ 计算活动预算 | `budget_calc.py`（费用模型） |
| ❌ 生成导航路径 | `navigation.py`（数据拼接） |
| ❌ 决策工作流顺序 | `workflow.py`（固定步骤） |

---

## 四、容错与鲁棒性设计

### 4.1 三层降级策略

```
Level 1: 最优路径
    LLM API 正常 → 完整的个性化策划案

Level 2: 部分降级
    LLM 返回异常 → 用模板数据 + 数据库教室 + 算法评分
    （只有标题和时间线用默认值，其他功能完全正常）

Level 3: 完全降级
    数据库也未初始化 → 手动运行 python data/init_db.py 恢复
```

### 4.2 关键容错点

| 组件 | 失败场景 | 处理方式 |
|------|---------|---------|
| LLM API | 网络超时 / Key 未配置 / 返回非 JSON | `_fallback_ideation()` 返回预设方案 |
| 数据库 | rooms.db 不存在 | `init_db.py` 可重建 |
| templates.json | 文件丢失 | `_default_template()` 返回硬编码模板 |
| 教室查询 | 无符合条件的教室 | 返回空列表，下游优雅处理 |

### 4.3 日志可追溯

每一步都打印到终端，方便调试和演示：

```
[Step 0] 意图解析: 讲座 50人
[Step 1] 匹配模板: 讲座
[Step 2] LLM创意: 前沿技术分享沙龙
[Step 3] 活动方案: 前沿技术分享沙龙
[Step 4] 查询到 6 间教室
[Step 5] 首选教室: D206
[Step 6] 导航已生成
[Step 7] 预算: 600元
[Step 8] HTML输出已生成
```

---

## 五、架构分层与职责

```
┌─────────────────────────────────────────────────┐
│  Web 展示层 (web/)                                │
│  ─────────────────                               │
│  职责: 用户交互、结果渲染                          │
│  LLM 依赖: ✗                                     │
│  文件: app.py, templates/index.html              │
├─────────────────────────────────────────────────┤
│  引擎层 (engine/)                                 │
│  ─────────────────                               │
│  职责: 工作流控制、步骤调度、核心决策              │
│  LLM 依赖: ✗                                     │
│  文件: workflow.py, intent_parser.py,            │
│         template_matcher.py, room_scorer.py      │
├─────────────────────────────────────────────────┤
│  工具层 (tools/)                                  │
│  ─────────────────                               │
│  职责: 纯数据计算，绝不调用外部服务                │
│  LLM 依赖: ✗                                     │
│  文件: db_service.py, navigation.py,            │
│         budget_calc.py                           │
├─────────────────────────────────────────────────┤
│  LLM 客户端 (llm_client.py)                       │
│  ─────────────────                               │
│  职责: 创意生成（仅 1 次调用）                     │
│  LLM 依赖: ✓ (但可降级)                           │
├─────────────────────────────────────────────────┤
│  数据层 (data/)                                   │
│  ─────────────────                               │
│  职责: 持久化存储                                 │
│  LLM 依赖: ✗                                     │
│  文件: init_db.py, templates.json, rooms.db      │
├─────────────────────────────────────────────────┤
│  配置 & 输出 (config.py, formatter.py)            │
│  ─────────────────                               │
│  职责: 环境配置、格式化输出                        │
│  LLM 依赖: ✗                                     │
└─────────────────────────────────────────────────┘
```

---

## 六、总结

### 6.1 设计原则

1. **LLM 是最小的依赖**：整个系统只在创意生成环节调用 LLM，其余全部是确定性代码
2. **数据 > 生成**：教室推荐来自数据库而非 LLM 编造，导航文本来自真实录入
3. **兜底无处不在**：LLM 不可用时系统照常运行，数据库为空时有一键初始化脚本
4. **可解释性**：每一步都打印日志，评分算法的每个维度都可追溯、可调整
5. **面向课程考核**：体现的是"我们设计的 Agent 架构"，而不是"我们调用了某个 API"

### 6.2 一句话总结

> **Campus Compass 的核心价值不是让用户和另一个 AI 聊天，而是我们自主设计的 8 步确定性引擎完成策划工作。LLM 只是一个被严格限定的创意辅助插件——它负责让方案更有个性化，但绝不负责决定方案内容本身。**
